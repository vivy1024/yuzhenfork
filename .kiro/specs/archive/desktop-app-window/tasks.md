# Tasks — Desktop App Window

- [x] 添加桌面窗口启动计划单元测试，覆盖禁用、Windows app mode、默认浏览器回退。
- [x] 实现 `packages/studio/src/api/desktop-window.ts`。
- [x] 在 `packages/studio/src/api/index.ts` 服务启动成功后打开应用窗口。
- [x] 更新根入口 `main.ts` 使用同一窗口启动逻辑。
- [x] 运行相关单元测试。
- [x] 运行 `bun run compile` 并验证编译产物保留嵌入前端资产。
