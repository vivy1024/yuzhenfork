/**
 * Novel plugin pages — writing workbench UI components.
 *
 * Re-exports the writing-workbench page so the studio shell can lazy-load it
 * from the plugin package boundary.
 */
export * from "./writing-workbench";
