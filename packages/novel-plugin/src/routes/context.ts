/**
 * Re-export RouterContext from studio — the route files depend on this type.
 * When services move to novel-plugin (Batch 4), this will become the canonical definition.
 */
export type { RouterContext } from "@vivy1024/novelfork-studio/api/routes/context";
