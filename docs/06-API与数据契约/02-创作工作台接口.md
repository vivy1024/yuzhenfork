# 创作工作台接口

**版本**: v1.0.1
**创建日期**: 2026-04-28
**更新日期**: 2026-05-06
**状态**: ✅ 当前有效
**文档类型**: current

---

## 本文定位

本文整理 NovelFork Studio 创作工作台当前已经存在的真实接口、透明过渡语义和仍未补齐的边界。它比 [Studio API总览](./01-Studio%20API总览.md) 更聚焦“作者在工作台里会直接碰到什么接口”，但仍然不是逐字段穷举 schema 文档。

接口口径必须反查 `packages/studio/src/api/routes/` 的真实实现；本文只记录当前事实，不把未来 spec 目标提前写成已完成。新前端访问这些能力时必须先经过 `packages/studio/src/app-next/backend-contract/` 的集中 contract client / adapter；组件内不得散写未登记 API 字符串，也不得把未登记能力渲染成可点击按钮。

## 状态术语

| 术语 | 含义 |
|---|---|
| `current` | route 已存在，且返回真实数据、真实持久化结果或真实错误 |
| `process-memory` | 只保存在当前进程内存，重启后不保证存在 |
| `prompt-preview` | 只返回 prompt / promptPreview，不代表已经生成或写入正文 |
| `chunked-buffer` | 已拿到完整 AI 结果后再切块发送，不是上游原生流式 |
| `unsupported` | 当前未接入，必须返回明确错误或透明占位，不得假成功 |
| `deprecated` | legacy route 仍存在但不允许新前端新增依赖；退役前必须先移除 client/docs/tests 引用并更新合同矩阵 |

## 创作工作台接口分组

| 分组 | 主要路径 | 当前口径 |
|---|---|---|
| 作品与章节 | `/api/books*`、`/api/project*` | current |
| 候选稿与草稿边界 | `/api/books/:id/candidates*`、`/api/books/:id/drafts*`、`/api/books/:id/draft` | candidates/drafts 资源 CRUD current；`/draft` 仍是 AI 异步动作 |
| 故事经纬与引导式生成 | `/api/books/:bookId/jingwei/*`、`/api/questionnaires*`、`/api/books/:bookId/core-shifts*` | current |
| AI 动作 | `/api/books/:id/write-next`、`/api/books/:id/audit/:chapter` 等 | current / transparent 混合 |
| 写作模式 | `/api/books/:bookId/inline-write` 等，`/api/books/:bookId/writing-modes/apply` | 生成接口以提示词预览为主；apply route 可安全落库 candidate/draft |
| 写作工具 | `/api/books/:bookId/hooks*`、`/api/books/:bookId/health` 等 | current / transparent 混合 |
| Provider 与模型池 | `/api/providers*` | current |
| 导出与运行事件 | `/api/books/:id/export`、`/api/books/:id/export-save`、`/api/events` | current / 部分 legacy |

## 1. 作品、章节与项目配置

### 作品读取与创建

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/books` | 列出作品 | 返回 `{ books }`，聚合 `book.json` 与章节索引统计 | 真实读取 |
| `GET` | `/api/books/:id` | 读取作品详情 | 返回 `{ book, chapters, nextChapter }`；不存在时 404 | 真实读取 |
| `POST` | `/api/books/create` | 创建作品 | 返回 `{ status: "creating", bookId, defaultSession, defaultSessionSnapshot }`；模型缺失时会走本地 scaffold fallback，而不是假正文 | 真实写入 |
| `GET` | `/api/books/:id/create-status` | 轮询建书状态 | 缺失返回 `{ status: "missing" }` + 404 | 当前进程状态 + 建书产物 |
| `PUT` | `/api/books/:id` | 更新作品配置 | 支持 `chapterWordCount`、`targetChapters`、`status`、`language` | 真实写入 |
| `DELETE` | `/api/books/:id` | 删除作品 | 删除本地目录与 SQLite `book` 记录 | 真实删除 |

### 章节读取、保存与状态

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/books/:id/chapters/:num` | 读取单章正文 | 返回 `{ chapterNumber, filename, content }`；找不到返回 404 | 真实读取 |
| `POST` | `/api/books/:id/chapters` | 新建章节 | 请求体 `{ title?: string, afterChapterNumber?: number }`；返回 `{ chapter: ChapterSummary }`；创建章节 Markdown 与章节索引 | 真实写入 |
| `PUT` | `/api/books/:id/chapters/:num` | 保存单章正文 | 请求体 `{ content, sessionId?, messageId?, toolUseId? }`；成功返回 `{ ok: true, chapterNumber, checkpointId? }`；正式覆盖写入前创建资源 checkpoint | 真实写入 |
| `DELETE` | `/api/books/:id/chapters/:num` | 删除单章 | 删除 Markdown 文件 + 从索引移除 | 真实删除 |
| `POST` | `/api/books/:id/chapters/:num/approve` | 标记章节通过 | 返回 `{ ok: true, status: "approved" }` | 真实写入章节索引 |
| `POST` | `/api/books/:id/chapters/:num/reject` | 回滚/拒绝章节 | 返回 `{ ok, status: "rejected", rolledBackTo, discarded }` | 真实写入 |
| `POST` | `/api/books/:id/import/chapters` | 导入章节文本 | 当前已存在真实 route，Dashboard 章节文本导入后可在 Workspace 资源树看到导入章节；失败返回真实错误 | 真实写入 |

> URL 章节导入尚未接入真实章节导入 route，Dashboard 当前以 disabled + 说明文案透明展示，不调用网页素材采风来冒充章节导入成功。

### Story / Truth 文件

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/books/:id/truth` | 列出 story 目录中的 truth 文件 | 返回 `{ files }`，包含 `name`、`size`、`preview` | 真实读取 |
| `GET` | `/api/books/:id/truth/:file` | 读取指定 truth 文件 | 非法文件名返回 400；不存在时返回 `{ file, content: null }` | 真实读取 |
| `PUT` | `/api/books/:id/truth/:file` | 保存指定 truth 文件 | 非法文件名返回 400 | 真实写入 |
| `DELETE` | `/api/books/:id/truth-files/:file` | 删除 truth 文件 | 非法文件名返回 400 | 真实删除 |
| `DELETE` | `/api/books/:id/story-files/:file` | 删除 story 文件 | 非法文件名返回 400 | 真实删除 |
| `GET` | `/api/project` | 读取项目配置 | 读取 `novelfork.json` | 真实读取 |

### Checkpoint 与 Rewind

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/books/:id/checkpoints/:checkpointId/rewind/preview` | 预览 checkpoint 恢复 | 返回 checkpoint 资源的 snapshot/current hash、diff、risk、currentExists/snapshotExists/changed；不写文件 | 真实读取 |
| `POST` | `/api/books/:id/checkpoints/:checkpointId/rewind/apply` | 应用 checkpoint 恢复 | 无 `confirmationDecision` 返回 pending confirmation；拒绝只写 audit；批准时校验 `expectedCurrentHashes`，先写 safety checkpoint，再恢复资源；冲突、snapshot 缺失或资源移动/删除返回真实错误 | 真实写入 |
| `PUT` | `/api/project` | 更新项目配置 | 写回 `novelfork.json` | 真实写入 |

## 2. 候选稿、草稿与 AI Draft 边界

### 候选稿 CRUD 与非破坏性处理

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/books/:id/candidates` | 列出候选稿 | 返回 `{ candidates }`，正文来自 `generated-candidates/*.md`；缺正文时该条返回 `content: null` 与 `contentError` | 真实读取 |
| `POST` | `/api/books/:id/candidates` | 新建候选稿 | 请求体至少需要 `title`、`content`、`source`；缺失返回 400 | 真实写入 |
| `POST` | `/api/books/:id/candidates/:candidateId/accept` | 接受候选稿 | `action` 必须是 `merge` / `replace` / `draft`；返回 `{ candidate }` 或 `{ candidate, draft }` | 真实写入 |
| `POST` | `/api/books/:id/candidates/:candidateId/reject` | 拒绝候选稿 | 返回状态更新结果 | 真实写入 |
| `POST` | `/api/books/:id/candidates/:candidateId/archive` | 归档候选稿 | 返回状态更新结果 | 真实写入 |

`accept` 的关键边界：

- `merge` / `replace` 会把候选正文写入正式章节。
- `draft` 会把候选正文写入 `books/<bookId>/drafts/<draftId>.md`，并返回新草稿对象。
- 任何非法 `action` 都返回 400，而不是静默吞掉。

### 当前草稿接口边界

草稿资源 CRUD 已补齐，当前可用接口：

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/books/:bookId/drafts` | 列出草稿资源 | 返回 `{ drafts }`，每条包含 `id`、`bookId`、`title`、`content`、`updatedAt`、`wordCount` | 真实读取 |
| `GET` | `/api/books/:bookId/drafts/:draftId` | 读取单个草稿 | 返回 `{ draft }`；不存在返回 404 | 真实读取 |
| `POST` | `/api/books/:bookId/drafts` | 新建草稿 | 请求体支持 `title`、`content`；返回 `{ draft }` | 真实写入 |
| `PUT` | `/api/books/:bookId/drafts/:draftId` | 更新草稿 | 请求体支持 `title`、`content`；返回 `{ draft }` | 真实写入 |
| `DELETE` | `/api/books/:bookId/drafts/:draftId` | 删除草稿 | 成功返回 `{ success: true }`；不存在返回 404 | 真实删除 |

候选稿接受为 `draft` 时会落盘到 `drafts/` 目录，并返回可由上述接口读取的新草稿。

`POST /api/books/:id/draft` 仍是 AI draft 异步动作启动接口，返回 `{ status: "drafting", bookId }`，不等同于草稿资源 CRUD。

## 3. 故事经纬与引导式生成

### 故事经纬结构化接口

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/books/:bookId/jingwei/sections` | 列出经纬栏目 | 返回 `{ sections }` | SQLite |
| `POST` | `/api/books/:bookId/jingwei/sections` | 新建栏目 | 缺 `key`/`name` 返回结构化错误 | SQLite |
| `PUT` | `/api/books/:bookId/jingwei/sections/:sectionId` | 更新栏目 | 找不到返回 404 | SQLite |
| `DELETE` | `/api/books/:bookId/jingwei/sections/:sectionId` | 删除栏目 | 软删除，返回 `{ ok: true, id }` | SQLite |
| `GET` | `/api/books/:bookId/jingwei/entries` | 列出条目 | 支持 `sectionId` 过滤 | SQLite |
| `POST` | `/api/books/:bookId/jingwei/entries` | 新建条目 | 缺 `sectionId`/`title` 返回错误 | SQLite |
| `PUT` | `/api/books/:bookId/jingwei/entries/:entryId` | 更新条目 | 返回 `{ entry }` | SQLite |
| `DELETE` | `/api/books/:bookId/jingwei/entries/:entryId` | 删除条目 | 软删除 | SQLite |
| `POST` | `/api/books/:bookId/jingwei/templates/apply` | 应用经纬模板 | 返回 `{ templateId, sections }`；已有栏目会跳过 | SQLite |
| `POST` | `/api/books/:bookId/jingwei/preview-context` | 预览经纬上下文 | 按章节与 tokenBudget 裁剪 | 即时装配 |

### 引导式生成接口

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/questionnaires` | 列出问卷模板 | 会先 seed 内置模板 | SQLite |
| `POST` | `/api/books/:bookId/questionnaires/:templateId/responses` | 创建问卷回答 | 返回 `{ response, targetObjectId }`；支持 `draft` / `skipped` / `submitted` | SQLite |
| `PUT` | `/api/books/:bookId/questionnaires/:templateId/responses/:id` | 更新问卷回答 | `status=submitted` 会重新走提交逻辑；其余状态走 update | SQLite |
| `POST` | `/api/books/:bookId/questionnaires/:templateId/ai-suggest` | 生成单题 AI 建议 | 模板/问题不存在返回 404 | 即时计算 |
| `POST` | `/api/books/:bookId/chapters/:chapter/pre-generation-questions` | 生成本章追问 | 返回问题和启发式命中，不写正文 | SQLite + 即时计算 |
| `GET` | `/api/books/:bookId/core-shifts` | 查询核心设定变更 | 可按状态过滤 | SQLite |
| `POST` | `/api/books/:bookId/core-shifts` | 提出核心设定变更 | 记录影响对象和章节 | SQLite |
| `POST` | `/api/books/:bookId/core-shifts/:id/accept` | 接受变更 | 返回 `{ coreShift }` | SQLite |
| `POST` | `/api/books/:bookId/core-shifts/:id/reject` | 拒绝变更 | 返回 `{ coreShift }` | SQLite |

## 4. AI 动作接口

### 当前已接入或已有真实 route 的动作

| 方法 | 路径 | 作用 | 当前边界 |
|---|---|---|---|
| `POST` | `/api/books/:id/write-next` | 生成下一章 | 当前返回 `{ status: "writing", bookId }` 启动异步流程，并通过 `/api/events` 广播 `write:*` 事件；不是同步返回候选稿正文 |
| `POST` | `/api/books/:id/draft` | 触发 AI draft | 返回 `{ status: "drafting", bookId }`；是异步动作启动，不等于草稿资源 CRUD |
| `POST` | `/api/books/:id/audit/:chapter` | 连续性/审校 | 成功直接返回审校结果；找不到章节 404 |
| `POST` | `/api/books/:id/revise/:chapter` | 修订草稿 | 成功直接返回 pipeline revise 结果 |
| `POST` | `/api/books/:id/rewrite/:chapter` | 重写章节 | 当前会先 rollback，再启动异步重写流程 |
| `POST` | `/api/books/:id/resync/:chapter` | 重建章节产物 | 返回 resync 结果 |
| `POST` | `/api/books/:id/detect/:chapter` | 单章 AI 痕迹检测 | 返回分析结果 |
| `POST` | `/api/books/:id/detect-all` | 全书 AI 痕迹检测 | 返回 `{ bookId, results }` |
| `POST` | `/api/ai/complete` | 行内补全 SSE | 返回 `chunked-buffer` 语义，不是上游原生流式 |
| `GET` | `/api/events` | Studio 全局 SSE | 广播 `write:*`、`draft:*`、`audit:*` 等事件 |

### 当前透明边界

- `write-next` 与 `draft` 是**异步动作入口**，当前文档不能把它们写成“同步返回正文”。
- `/api/ai/complete` 会在拿到完整结果后再分块发送，SSE payload 含 `streamSource: "chunked-buffer"`。
- AI 失败时返回真实错误或 SSE `error` event，不能伪造成功结果。

## 5. 写作模式接口

当前写作模式已接入 LLM 真实生成。无 session LLM 时优雅降级为 prompt-preview。

| 方法 | 路径 | 结果 | 当前口径 |
|---|---|---|---|
| `POST` | `/api/books/:bookId/inline-write` | `mode: "generated"` + `content` + `promptPreview` + `usage` | continuation / expansion / bridge 真实生成 |
| `POST` | `/api/books/:bookId/dialogue/generate` | `mode: "generated"` + `content` | 对话生成真实生成 |
| `POST` | `/api/books/:bookId/variants/generate` | `mode: "generated"` + `variants[]` | 多版本真实生成 |
| `POST` | `/api/books/:bookId/outline/branch` | `mode: "generated"` + `content` | 大纲分支真实生成 |
| `POST` | `/api/books/:bookId/writing-modes/apply` | `{ target, requestedTarget, resourceId, status, metadata }` | 安全写入 candidate/draft |
| `POST` | `/api/works/import` | 解析导入文本 | 只返回解析结果，不直接落库 |
| `GET` | `/api/style/personal-profile` | 合并个人文风 profile | `profiles` 缺失或 JSON 非法返回 400 |
| `POST` | `/api/books/:bookId/style/drift-check` | 返回 `drift` | 真实分析结果 |

`POST /api/books/:bookId/writing-modes/apply` 已提供安全应用 route：可以写入候选稿或草稿；正式章节 insert/replace 首版会转为候选稿，避免无确认地改动正式正文。Workspace UI 已接入预览、目标选择、确认和写入结果展示；生成接口在有 session LLM 时返回真实 `mode: "generated"` 结果，无 session LLM 或模型不可用时降级为 `prompt-preview`，因此 prompt-preview 不代表真实写入，只代表透明预览。

## 6. 写作工具接口

| 方法 | 路径 | 作用 | 当前响应/边界 |
|---|---|---|---|
| `POST` | `/api/books/:bookId/hooks/generate` | 生成章节 hooks | 模型不可用时返回结构化错误 + `code: MODEL_NOT_CONFIGURED` + `capability: hooks.generate` + `gate`，HTTP 409 |
| `POST` | `/api/books/:bookId/hooks/apply` | 持久化 hook | 成功返回 `{ persisted: true, file: "pending_hooks.md", hookId }` |
| `GET` | `/api/books/:bookId/pov` | POV 仪表盘 | 基于 story 文件构建 dashboard |
| `GET` | `/api/progress` | 写作进度 | 返回 `{ config, progress, trend }` |
| `PUT` | `/api/progress/config` | 更新进度配置 | 返回 `{ config }` |
| `POST` | `/api/books/:bookId/chapters/:ch/rhythm` | 节奏分析 | 缺正文返回 404 |
| `POST` | `/api/books/:bookId/chapters/:ch/dialogue` | 对话分析 | 缺正文返回 404 |
| `GET` | `/api/books/:bookId/health` | 书籍健康面板 | 真实字段返回 measured metric；当前部分指标可能返回 `null`，前端需映射为未知状态 |
| `GET` | `/api/books/:bookId/conflicts/map` | 冲突图 | 返回 `{ conflicts }` |
| `GET` | `/api/books/:bookId/arcs` | 人物弧 | 返回 `{ arcs }` |
| `POST` | `/api/books/:bookId/chapters/:ch/tone-check` | 语气漂移检查 | 返回 `{ result }` |

`/api/books/:bookId/health` 是当前“真实计算 + 空值未知”混合的典型接口：

- `totalChapters`、`totalWords`、`dailyWords`、`dailyTarget`、`sensitiveWordCount`、`knownConflictCount` 来自真实数据。
- `consistencyScore`、`hookRecoveryRate`、`aiTasteMean`、`rhythmDiversity` 当前在无数据或计算失败时可能返回 `null`；前端必须把 `null` 映射为未知/待接入，不能写成满分、0 或绿色成功。

## 7. Provider、模型池与 AI gate

Provider routes 由 `createProvidersRouter()` 挂载到 `/api/providers`：

| 方法 | 路径 | 作用 | 当前响应/边界 |
|---|---|---|---|
| `GET` | `/api/providers` | 列出 provider | 返回脱敏后的 `{ providers }` |
| `GET` | `/api/providers/status` | 返回运行时 provider 状态 | 基于 runtime store |
| `GET` | `/api/providers/summary` | 返回供应商运行态统计 | 拆分 provider total、enabled provider、available models、total catalog models、callable models、platform accounts 与 issue count |
| `GET` | `/api/providers/models/grouped` | 返回按 provider 分组的物理模型库存 | 能力标签只从真实 inventory 派生：大上下文、多模态、思考链、工具调用；未知为 `unknown` |
| `GET` | `/api/providers/models` | 统一模型池 | 只返回 enabled provider/model/可用账号组合 |
| `POST` | `/api/providers` | 新建 provider | 缺 `id/name/type` 返回 400 |
| `PUT` | `/api/providers/:id` | 更新 provider | 不存在返回 404 |
| `POST` | `/api/providers/:id/toggle` | 启停 provider | 返回脱敏 provider |
| `POST` | `/api/providers/reorder` | 调整顺序 | 非法 id 返回 400 |
| `POST` | `/api/providers/:id/models/refresh` | 刷新模型列表 | adapter unsupported / auth 缺失返回失败 envelope |
| `PATCH` | `/api/providers/:id/models/:modelId` | patch 单模型 | 不存在返回 404 |
| `POST` | `/api/providers/:id/models/:modelId/test` | 测试单模型 | 写回 runtime model `lastTestStatus/lastTestError`；失败时返回 envelope + model；成功返回 `{ success: true, latency, model }` |
| `POST` | `/api/providers/:id/test` | 测试 provider 当前启用模型 | 使用第一个启用模型并写回 runtime model 测试状态；无启用模型返回 400 |
| `DELETE` | `/api/providers/:id` | 删除 provider | 成功返回 `{ success: true }` |

关键边界：

- 当前 runtime model pool 已是唯一运行时模型来源；summary 中 `availableModelCount` 来自可进入 runtime model pool 的模型，`callableModelCount` 只统计最近测试成功的可调用模型。
- `/api/providers` 不回传明文 `apiKey`，通过 `apiKeyConfigured` 表示密钥状态；平台集成账号的 `credentialJson` 脱敏边界属于 `/api/platform-integrations` 视图。
- provider/model test 状态以 runtime store 为准：模型测试和 provider 当前启用模型测试都会写回真实 `lastTestStatus`、`lastTestLatency` 或 `lastTestError`。
- adapter 未实现时必须返回真实失败 envelope，而不是伪造模型可用。
- provider adapter 失败统一使用 `{ success: false, code, error, capability? }`：`unsupported` 返回 501，`auth-missing` / `config-missing` 返回 422，`upstream-error` / `network-error` 返回 502，未知失败返回 500。

## 8. 平台集成 unsupported 边界

`/api/platform-integrations/:platformId/accounts/import-json` 对未接入的 JSON 导入能力必须返回统一 unsupported failure：`{ success: false, code: "unsupported", capability, error }` + HTTP 501，不能用空账号、空对象或假成功替代。

## 9. 导出与 legacy route 依赖调查

Task 9 全仓搜索确认：轻量 `/api/chat/:bookId/*` 与 exact `POST /api/agent` 已退役；以下保留或已退役入口不能再被新前端当作未登记 current 能力直接调用，保留项必须带透明状态，候选退役项在删除前还要再次确认外部调用方。

| 入口 | 搜索到的依赖 | 当前标记 | 当前口径 / 后续动作 |
|---|---|---|---|
| `GET /api/books/:id/export` | API 文档、Studio README；无 app-next current contract client | `deprecated` | 真实读取导出内容，但属于 legacy 导出读取路径。 |
| `POST /api/books/:id/export` | 创作流程、API 文档、Studio README、server integration tests | `deprecated` | 真实生成 Markdown/TXT 导出；统一导出范围选择与新前端 contract 尚未收敛。 |
| `POST /api/books/:id/export-save` | API 文档与 route 定义 | `deprecated` | 保存导出结果；保留到统一导出 contract/client 落地。 |
| `GET /api/pipeline*` | `server.ts` mount 与 API 总览 | `process-memory` / transparent | 可用于调试当前进程 run/stage 状态；不得描述成持久化运行历史。 |
| `GET /api/monitor/status`、`WS /api/monitor/logs` | `monitor.test.ts`、mock debt ledger、API 总览；旧 `MonitorWidget` 未挂载 | `unsupported` | 无 daemon/runtime 事实源时返回或发送 unsupported；不得伪造 stopped 或实时日志。 |
| retired `POST /api/agent` | Task 9 已从 `createAIRouter` 移除；`/api/agent/config` 是独立 current route | `unsupported` / retired | 不新增消费者；新入口使用 `/api/sessions`、`/api/exec` 或 session tools。 |
| `POST /api/ai/transform`、`/api/ai/context-assembly`、`/api/ai/outline` | 旧 `ContextPanel` / `OutlinePanel` 及测试；app-next current contract 主线不依赖 | `deprecated` | 后续迁入 session tools、resource adapter 或 writing action adapter；禁止新增直连组件依赖。 |
| unmounted `/api/poison-detector*`、`/api/hooks*` | `tsconfig` exclude 与 `routes/index.ts`/`server.ts` 注释导出 | `unsupported` / unmounted | 保持不挂载；恢复前必须重构为 Hono/current route 并补合同。 |

当前工作台已有导出相关 route；现阶段文档必须写清楚：**导出可作为真实 legacy 能力保留，但统一导出入口、范围选择和新前端呈现还未按 Backend Contract 收敛。**

## 10. Backend Contract 与前端重建边界

`.kiro/specs/backend-contract-v1/` 是新前端重建前的能力合同入口，`.kiro/specs/frontend-refoundation-v1/` 是后续前端实现主线。前端不得继续按旧三栏 UI 的想象直接接 API，而必须先通过合同矩阵确认能力状态。

首版合同矩阵把创作工作台能力分成以下 UI 可依赖组：

| 组 | 前端用途 | 合同入口 |
|---|---|---|
| 启动与侧栏 | 书籍列表、会话列表、provider/model 状态、进度 | `/api/books`、`/api/sessions`、`/api/providers*`、`/api/progress` |
| Agent Shell | 会话快照、WebSocket、ack/replay、工具确认门、session config 更新回读、continue latest、fork、restore、compact、memory 边界、worktree/Git 状态摘要 | `/api/sessions/:id`、`/api/sessions/lifecycle/latest`、`/api/sessions/:id/fork`、`/api/sessions/:id/restore`、`/api/sessions/:id/compact`、`/api/sessions/:id/memory/status`、`/api/sessions/:id/memory`、`/api/sessions/:id/chat/state`、`/api/sessions/:id/chat/history`、`WS /api/sessions/:id/chat`、`/api/sessions/:id/tools*`、`/api/worktree/status?path=` |
| Writing Workbench | 章节、候选稿、草稿、story/truth、经纬、叙事线 | `/api/books/:id*`、`/api/books/:id/candidates*`、`/api/books/:id/drafts*`、`/api/books/:bookId/jingwei/*`、`/api/books/:bookId/narrative-line*` |
| AI 写作动作 | session-native 写下一章、writing modes、hooks、审校/检测 | `cockpit.*`、`pgi.*`、`guided.*`、`candidate.create_chapter`、`/api/books/:id/write-next`、`/api/books/:bookId/writing-modes/*` |
| Provider 与模型 | provider 管理、模型刷新、模型测试、工具支持降级 | `/api/providers*` |

前端规则：

- 新前端只能通过 `createContractClient`、`fetch-json-contract-client` 及 `session/resource/provider/writing action` 领域 client、`resource-tree-adapter`、`writing-action-adapter`、session WebSocket helper 访问登记能力；若需要新增 route，先补 Backend Contract 矩阵、共享类型和 contract 测试，再接 UI。
- 组件内不得散写 fetch 字符串调用未登记 API；不得用 mock/fake/noop 数据填充 contract client 失败结果。`backend-contract-verification.ts` 的 app-next API guard 是当前收口检查来源。
- `current` 能力可启用，但仍要展示真实错误。
- `process-memory` 必须标注临时状态。
- `prompt-preview` 只允许预览/复制/显式 apply。
- `chunked-buffer` 可显示流式体验，但不能写成上游原生流式。
- `unsupported` 必须禁用或显示失败说明。
- `null` 指标或 `unknown` metric 必须显示未知/待接入，不得伪装为绿色成功。

## 11. 当前缺口与禁止写法

### 当前缺口

1. writing modes 安全 apply route 已落地；生成接口在有 session LLM 时返回真实生成结果，无 session LLM 或模型不可用时降级为 prompt-preview，prompt-preview 只可查看/复制。
2. `write-next` / `draft` 仍是异步启动接口，不是同步正文接口。
3. 工作台主入口已转为右侧叙述者会话；历史 Workspace AI action route map 仍可复用，但驾驶舱 / 经纬 / 写作能力应优先作为 session tools、工具结果卡片或中间画布组件呈现。
4. 导出仍是当前 route 与后续 spec 设计并存阶段。
5. 删除类 destructive 能力当前保持硬删除语义：book/chapter/story file/truth file/candidate/draft 由 service 执行真实文件或索引删除，route adapter 保留前端确认入口、旧响应 envelope 与不存在/非法文件名失败状态。
6. session runtime 已抽出 transport/recovery 内聚 helper，但外部合同不变：WebSocket `session:snapshot` / `session:state` / `session:message` / `session:error` envelope、REST snapshot/history、ack、resumeFromSeq、resetRequired 与 confirmation API 仍以共享类型和现有 route 为准。
7. session memory 当前已接入写入边界 service 和 route/client/matrix：用户偏好写 user scope，项目事实写 project scope 且必须带 project-resource 来源，临时剧情草稿默认返回 skipped；默认 writer 未配置时 `GET /api/sessions/:id/memory/status` 返回 readonly，UI 必须显示未接入写入器而不是假成功。
8. session config 更新必须走真实 `PUT /api/sessions/:id`，成功后前端回读 `GET /api/sessions/:id/chat/state` 并同步 ShellDataProvider；plan session 对 `allow/edit` 权限更新返回 `UNSUPPORTED_PERMISSION_MODE` 结构化错误，不能只在 UI 本地拦截。
9. narrator route 的工作区/Git 摘要只能来自真实 session `worktree` 与 `/api/worktree/status?path=`；不可读、非 Git 或无 worktree 时显示 unavailable reason，不渲染静态 Git 面板。
10. 对话工具卡必须在 DOM、raw payload 与复制文本中脱敏 secret；pending confirmation UI 必须展示来自真实 pending confirmation/tool policy 流程的目标、风险、来源与操作，不把未接 Retry/Clear/Fork/Resume 渲染成可点击 current 动作。

### 禁止写法

- 不得把 `prompt-preview` 写成“已生成正文”。
- 不得把 `process-memory` 描述成“重启后仍保留”的长期状态。
- 不得把 `unsupported` 写成“当前可用”。
- 不得把模型不支持工具调用时的 `unsupported-tools` 写成工具已执行；正确降级是只读解释、prompt-preview 或引导用户切换支持 tools 的模型。
- 不得把 OpenAI-compatible 上游 function name 约束忽略掉；session tool 内部名可包含点号，但发给上游的工具名必须经过 provider-safe 映射，收到 tool call 后再还原为内部工具名。
- 不得把异步 `POST /api/books/:id/draft` 写成草稿资源 CRUD；草稿资源 CRUD 以 `/api/books/:id/drafts*` 为准。
- 不得把临时剧情草稿自动写入长期 memory；项目事实没有 project-resource 来源时不得写入 project memory，writer 未配置时不得返回假成功。

## 12. 验证事实源

合同层验证收口同时依赖 `packages/studio/src/app-next/backend-contract/backend-contract-verification.ts` 记录的 Backend Contract 聚焦测试、Studio typecheck、docs verify、`git diff --check` 与 app-next API guard；未运行项不得在报告中标记为通过。`backend-core-refactor-v1` Task 10 收口验证使用后端合同/storage/session/provider/legacy route 聚焦回归、Studio typecheck 和 docs verify，不把未运行的完整 GUI 冒烟写成通过。

- `packages/studio/src/api/routes/storage.ts`
- `packages/studio/src/api/lib/books-service.ts`
- `packages/studio/src/api/lib/books-service.test.ts`
- `packages/studio/src/api/lib/story-file-service.ts`
- `packages/studio/src/api/lib/story-file-service.test.ts`
- `packages/studio/src/api/lib/storage-write-service.ts`
- `packages/studio/src/api/lib/storage-write-service.test.ts`
- `packages/studio/src/api/lib/storage-destructive-service.ts`
- `packages/studio/src/api/lib/storage-destructive-service.test.ts`
- `packages/studio/src/api/lib/candidate-destructive-service.ts`
- `packages/studio/src/api/lib/candidate-destructive-service.test.ts`
- `packages/studio/src/api/routes/chapter-candidates.ts`
- `packages/studio/src/api/routes/session.ts`
- `packages/studio/src/api/routes/session.test.ts`
- `packages/studio/src/api/lib/session-chat-service.ts`
- `packages/studio/src/api/lib/session-chat-service.test.ts`
- `packages/studio/src/api/lib/session-runtime/transport.ts`
- `packages/studio/src/api/lib/session-runtime/transport.test.ts`
- `packages/studio/src/api/lib/session-runtime/recovery.ts`
- `packages/studio/src/api/lib/session-runtime/recovery.test.ts`
- `packages/studio/src/api/routes/ai.ts`
- `packages/studio/src/api/routes/bible.ts`
- `packages/studio/src/api/routes/jingwei.ts`
- `packages/studio/src/api/routes/writing-modes.ts`
- `packages/studio/src/api/errors.ts`
- `packages/studio/src/api/errors.test.ts`
- `packages/studio/src/api/routes/writing-tools.ts`
- `packages/studio/src/api/routes/providers.ts`
- `packages/studio/src/api/routes/platform-integrations.ts`
- [Studio API总览](./01-Studio%20API总览.md)
