# 小说后端架构

NovelFork 后端由两层构成：`packages/core/` 提供核心写作引擎（Agent 管线、经纬系统、过滤器、合规检查、预设库），`packages/studio/src/api/` 提供 Hono HTTP 路由层和 Agent 运行时。数据持久化使用 SQLite（bun:sqlite），AI 调用通过 LlmRuntimeService 统一路由到多供应商。

---

## 模块地图

```
packages/core/src/
├── agents/          — 写作 Agent 集合（20+ 个专业 Agent）
├── pipeline/        — 多 Agent 写作管线编排
├── jingwei/         — 经纬系统（世界观/角色/事件/设定管理）
├── filter/          — AI 味过滤器（规则引擎 + 朱雀 API）
├── compliance/      — 平台合规检查（敏感词/AI 声明/格式）
├── presets/         — 写作预设库（流派/文风/节拍/逻辑风险）
├── tools/           — 写作工具集（节奏/对话/进度/弧线/冲突/钩子）
├── storage/         — SQLite 存储层（schema + migrations + repositories）
├── state/           — 状态管理（StateManager + MemoryDB + projections）
├── hooks/           — 生命周期钩子系统
├── models/          — 数据模型定义
├── llm/             — LLM 客户端抽象
├── mcp/             — MCP 协议客户端
├── notify/          — 通知分发（飞书/企微/Telegram/Webhook）
├── plugins/         — 插件系统
├── registry/        — 工具注册表 + 命令注册表
├── relay/           — 本地中继
├── runtime/         — 进程适配器
└── utils/           — 工具函数集

packages/studio/src/api/
├── routes/          — Hono HTTP 路由（40+ 路由文件）
└── lib/             — 运行时服务层
    ├── agent-turn-runtime.ts    — Agent 循环运行时
    ├── session-chat-service.ts  — 会话聊天服务（WebSocket）
    ├── llm-runtime-service.ts   — LLM 统一调用层
    ├── provider-adapters/       — 供应商适配器
    └── session-tool-executor.ts — 工具执行器
```

---

## API 路由清单

### 核心写作路由

| 路由文件 | 前缀 | 功能 |
|---------|------|------|
| `pipeline.ts` | `/api/pipeline/` | 写作管线执行（plan/compose/write/revise） |
| `runs.ts` | `/api/runs/` | 管线运行记录 |
| `bible.ts` | `/api/books/:id/bible/` | 经纬资料 CRUD（旧接口） |
| `jingwei.ts` | `/api/books/:id/jingwei/` | 经纬系统新接口 |
| `chapter-candidates.ts` | `/api/books/:id/candidates/` | 候选稿管理 |
| `writing-tools.ts` | `/api/books/:id/...` | 写作工具（节奏/对话/健康/弧线/冲突） |
| `writing-modes.ts` | `/api/books/:id/inline-write` | 选段写作（续写/扩写/补写） |
| `filter.ts` | `/api/filter/` | AI 味检测 |
| `compliance.ts` | `/api/compliance/` | 合规检查 + 导出 |
| `presets.ts` | `/api/presets/` | 预设管理 |
| `narrative-line.ts` | `/api/books/:id/narrative-lines/` | 叙事线管理 |
| `golden-chapters.ts` | `/api/books/:id/golden-chapters/` | 黄金章节标记 |
| `rhythm.ts` | `/api/books/:id/chapters/:ch/rhythm` | 段落节奏分析 |
| `onboarding.ts` | `/api/onboarding/` | 新书引导向导 |

### 运行时路由

| 路由文件 | 前缀 | 功能 |
|---------|------|------|
| `session.ts` | `/api/sessions/` | 会话 CRUD + WebSocket 聊天 |
| `ai.ts` | `/api/ai/` | AI 直接调用 |
| `ai-relay.ts` | `/api/ai-relay/` | AI 中继代理 |
| `providers.ts` | `/api/providers/` | 供应商管理 |
| `agent-config.ts` | `/api/agent-config/` | Agent 配置 |
| `runtime-capabilities.ts` | `/api/runtime/capabilities` | 运行时能力查询 |
| `runtime-status.ts` | `/api/runtime/status` | 运行时状态 |
| `monitor.ts` | `/api/monitor/` | 监控面板 |
| `exec.ts` | `/api/exec/` | 命令执行 |
| `terminals.ts` | `/api/terminals/` | 终端管理 |

### 基础设施路由

| 路由文件 | 前缀 | 功能 |
|---------|------|------|
| `storage.ts` | `/api/storage/` | 存储操作 |
| `settings.ts` | `/api/settings/` | 用户设置 |
| `auth.ts` | `/api/auth/` | 认证 |
| `search.ts` | `/api/search/` | 全文搜索 |
| `upload.ts` | `/api/upload/` | 文件上传 |
| `snapshots.ts` | `/api/books/:id/checkpoints/` | 快照与回滚 |
| `workspace-management.ts` | `/api/workspace/` | 工作区管理 |
| `context-manager.ts` | `/api/context/` | 上下文管理 |
| `learning.ts` | `/api/learning/` | 学习中心 |
| `mcp.ts` | `/api/mcp/` | MCP 服务器管理 |

---

## 服务层

### Agent 运行时三层架构

```
┌─────────────────────────────────────────────────────┐
│  session-chat-service.ts (会话管理层)                 │
│  - WebSocket 连接管理                                │
│  - 消息队列 + 序列号                                 │
│  - 状态广播 (working/idle/retrying)                  │
│  - 自动压缩 + 自动命名                              │
│  - 确认决策流                                        │
├─────────────────────────────────────────────────────┤
│  agent-turn-runtime.ts (Agent 循环层)                │
│  - generate → tool_use → execute → loop             │
│  - 并行工具执行 (Read/Glob/Grep 等只读工具)          │
│  - 重复工具调用拦截                                  │
│  - 上下文溢出恢复 (紧急截断 + 重试)                  │
│  - 连续失败检测 + 静默工具调用阈值                   │
│  - 工具结果截断 (30K 字符上限)                       │
├─────────────────────────────────────────────────────┤
│  llm-runtime-service.ts (LLM 调用层)                │
│  - 多供应商 fallback (primary → same-model peers)    │
│  - 聚合模型解析                                      │
│  - 指数退避重试 (429/502/503)                        │
│  - 协议自动推断 (OpenAI/Anthropic compatible)        │
│  - 流式输出 + 工具调用解析                           │
└─────────────────────────────────────────────────────┘
```

### 写作管线服务

```
PipelineRunner (packages/core/src/pipeline/runner.ts)
├── ArchitectAgent     — 架构设计（全书大纲）
├── PlannerAgent       — 章节规划（意图 + 冲突）
├── ComposerAgent      — 上下文编排（经纬注入）
├── WriterAgent        — 正文写作
├── LengthNormalizerAgent — 字数规范化
├── ChapterAnalyzerAgent  — 章节分析
├── ContinuityAuditor  — 连续性审计
├── ReviserAgent       — 修订
├── StateValidatorAgent — 状态验证
├── RadarAgent         — 雷达扫描（序列级问题）
└── DetectionRunner    — AI 味 + 敏感词检测
```

---

## 数据流：用户动作 → 存储 → AI

### 写下一章完整链路

```
用户点击"写下一章"
  │
  ▼
[1] GET /api/books/:id/cockpit/snapshot
    → 获取当前书籍状态 + 进度
  │
  ▼
[2] POST /api/books/:id/pgi/generate-questions
    → PGI 引擎分析经纬数据，生成追问
    → 用户回答追问（或跳过）
  │
  ▼
[3] POST /api/books/:id/guided/enter
    → 形成 GuidedGenerationPlan
    → 用户批准计划
  │
  ▼
[4] POST /api/books/:id/candidates/create-chapter
    → PipelineRunner 执行:
      a. buildJingweiLegacyContext() → 注入经纬上下文
      b. PlannerAgent.plan() → 章节意图
      c. ComposerAgent.compose() → 完整上下文包
      d. WriterAgent.write() → 初稿
      e. ContinuityAuditor.audit() → 连续性检查
      f. ReviserAgent.revise() → 修订（如有问题）
      g. StateValidatorAgent.validate() → 状态验证
    → 结果写入 candidates 表
  │
  ▼
[5] 用户在画布中查看候选稿
    → 接受 → 写入 chapters 表
    → 拒绝 → 标记 rejected
```

### AI 味检测链路

```
用户选择章节 → 点击"AI 味检测"
  │
  ▼
POST /api/filter/scan { text, bookId, chapterNumber, persist: true }
  │
  ▼
filter/engine/rules.ts — 12 条本地规则扫描
  ├── 句式重复检测
  ├── 过度修饰检测
  ├── 情感标签堆砌
  ├── 转折词滥用
  └── ...
  │
  ▼
filter/zhuque/client.ts — 朱雀 API 远程检测（可选）
  │
  ▼
filter/suggestions/seven-tactics.ts — 生成修复建议
  │
  ▼
filter/repositories/filter-report-repo.ts — 持久化报告
  │
  ▼
返回 FilterReport { score, issues[], suggestions[] }
```

---

## 数据库表（小说相关）

### 经纬系统表

| 表名 | 用途 |
|------|------|
| `jingwei_book` | 书籍元数据（模式/当前章节） |
| `jingwei_character` | 角色（别名/特征/可见性规则） |
| `jingwei_event` | 事件（类型/章节范围/伏笔状态） |
| `jingwei_setting` | 设定（分类/嵌套引用） |
| `jingwei_chapter_summary` | 章节摘要（滑动窗口 15 章） |
| `jingwei_conflict` | 矛盾（主次/利害/演化路径） |
| `jingwei_world_model` | 世界模型（经济/社会/地理/力量体系） |
| `jingwei_premise` | 故事基线（logline/基调/目标读者） |
| `jingwei_character_arc` | 角色弧线（起止状态/转折点） |
| `jingwei_cooccurrence` | 共现矩阵（实体对共现计数） |
| `jingwei_volume_summaries` | 卷级摘要（每 30 章压缩） |
| `jingwei_causal_chains` | 因果链（触发事件/紧迫度） |
| `story_jingwei_section` | 经纬分区（新系统） |
| `story_jingwei_entry` | 经纬条目（新系统，结构化字段） |

### 写作管线表

| 表名 | 用途 |
|------|------|
| `chapters` | 已确认章节 |
| `chapter_candidates` | 候选稿 |
| `pipeline_runs` | 管线运行记录 |
| `filter_reports` | AI 味检测报告 |
| `daily_progress` | 日更进度 |
| `audit_logs` | 审计日志 |

### 会话与运行时表

| 表名 | 用途 |
|------|------|
| `sessions` | 叙述者会话 |
| `session_messages` | 会话消息历史 |
| `kv_store` | 通用键值存储 |
| `user_templates` | 用户自定义模板 |

---

## Agent 集合

| Agent | 文件 | 职责 |
|-------|------|------|
| ArchitectAgent | `architect.ts` | 全书架构设计 |
| PlannerAgent | `planner.ts` | 单章规划（意图/冲突/节拍） |
| ComposerAgent | `composer.ts` | 上下文编排（经纬 + 规则 + 记忆） |
| WriterAgent | `writer.ts` | 正文写作 |
| ReviserAgent | `reviser.ts` | 修订（基于审计结果） |
| ContinuityAuditor | `continuity.ts` | 连续性审计 |
| StateValidatorAgent | `state-validator.ts` | 状态一致性验证 |
| ChapterAnalyzerAgent | `chapter-analyzer.ts` | 章节质量分析 |
| LengthNormalizerAgent | `length-normalizer.ts` | 字数规范化 |
| RadarAgent | `radar.ts` | 序列级问题扫描 |
| DetectorAgent | `detector.ts` | AI 味检测 Agent |
| InlineWriterAgent | `inline-writer.ts` | 选段写作 |
| VariantGeneratorAgent | `variant-generator.ts` | 多版本变体 |
| DialogueGeneratorAgent | `dialogue-generator.ts` | 对话生成 |
| OutlineBrancherAgent | `outline-brancher.ts` | 大纲分支 |
| StyleAnalyzerAgent | `style-analyzer.ts` | 文风分析 |
| ConsolidatorAgent | `consolidator.ts` | 状态整合 |
| FoundationReviewerAgent | `foundation-reviewer.ts` | 基础审查 |
| PostWriteValidatorAgent | `post-write-validator.ts` | 写后验证 |
| FanficCanonImporter | `fanfic-canon-importer.ts` | 同人原作导入 |
