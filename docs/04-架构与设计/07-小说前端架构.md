# 小说前端架构

NovelFork Studio 的写作工作台（Writing Workbench）是一个 Agent-native 的本地 Web 工作台，采用左资源栏 / 中间画布 / 右侧叙述者会话的三栏布局。前端基于 React 19 + TypeScript + Tailwind CSS + shadcn/ui 构建，通过 REST API 和 WebSocket 与后端通信。

---

## 组件树

```
WritingWorkbenchRoute (顶层路由组件)
├── header (顶部标题栏)
│   ├── BookOpen icon + bookTitle + statusLabel
│   ├── 视图切换 (资源树 / 章节图)
│   ├── writingActions slot (WorkbenchWritingActions)
│   ├── + 新建章节 按钮
│   ├── 写作工具 按钮 → Dialog(WritingToolsPanel)
│   ├── 快照 按钮 → CheckpointPanel
│   └── 经纬 按钮 → Dialog(JingweiPanel)
├── ChapterGraph (图视图模式)
└── 内容区 (树视图模式)
    ├── WorkbenchResourceTree (左侧资源树, w-64)
    └── WorkbenchCanvas (中间画布)
        ├── CockpitOverview (无选中节点时)
        │   └── NewBookGuide (新书引导向导)
        ├── CandidateActionsBar (候选稿操作栏)
        ├── JingweiEntryEditor (经纬条目编辑器)
        └── ResourceViewer (通用资源查看器)
            └── ChapterEditor (章节编辑器, 含 Tab 补全)
```

### 对话框面板

```
WritingToolsPanel (写作工具面板, Dialog)
├── Tabs: 工具 / 节拍表 / 预设
├── 工具 Tab
│   ├── InlineWritePanel (选段写作: 续写/扩写/补写)
│   ├── VariantsPanel (多版本变体)
│   ├── CharacterArcsPanel (角色弧线)
│   ├── StyleDriftPanel (文风漂移检测)
│   ├── TemplateMarketPanel (模板市场)
│   ├── WRITING_TOOLS grid (8 个工具按钮)
│   ├── AiTasteReport (AI 味检测报告)
│   ├── ChapterHealthCard (章节健康度)
│   ├── CompliancePanel (发布合规检查)
│   └── ExportPanel (导出 TXT/Word/ePub)
├── 节拍表 Tab → BeatTemplateList
└── 预设 Tab → PresetsPanel

JingweiPanel (经纬资料管理, Dialog)
├── JingweiCategorySidebar (分类侧栏)
├── JingweiEntryList / JingweiEntryTree (条目列表/树)
├── JingweiEntryForm (条目编辑表单)
├── JingweiGraphView (关系图)
└── JingweiProgressions (进度追踪)
```

---

## 数据流

### 资源加载

```
后端 /api/books/:bookId/resources
  → loadResourceTreeFromContract()
    → buildWorkbenchResourceTree()
      → useWorkbenchResources() hook
        → { tree, resourceMap, openableNodes, errors }
```

### 资源详情按需加载

```
用户点击资源节点
  → ResourceDetailLoader.resourceNeedsDetailHydration(node)
  → 若需要: fetch /api/books/:bookId/resources/:id/detail
  → 更新 node.content + node.metadata
```

### 保存流程

```
用户编辑 → setContent() → setDirty(true)
  → 点击保存 → onSave(node, content)
    → ResourceSaveController
      → PATCH /api/books/:bookId/resources/:id
      → setDirty(false)
```

### Tab 补全 (续写)

```
用户按 Tab → onTabComplete(currentContent, cursorPosition)
  → POST /api/books/:bookId/inline-write
    body: { mode: "continuation", context: last500chars, maxTokens: 80 }
  → 返回 { text } → 插入光标位置
```

---

## 面板清单

| 面板 | 文件 | 功能 | 调用 API |
|------|------|------|----------|
| AiTasteReport | `AiTasteReport.tsx` | AI 味检测报告展示 | POST `/api/filter/scan` |
| BeatProgressBar | `BeatProgressBar.tsx` | 节拍模板列表 + 进度条 | 纯前端数据 |
| BookHealthSummary | `BookHealthSummary.tsx` | 全书健康度汇总 | GET `/api/books/:id/health` |
| CandidateActionsBar | `CandidateActionsBar.tsx` | 候选稿接受/拒绝/归档/删除 | POST `/api/books/:id/candidates/:cid/...` |
| ChapterHealthCard | `ChapterHealthCard.tsx` | 单章节奏+对话分析 | POST `/api/books/:id/chapters/:ch/rhythm` |
| CharacterArcsPanel | `CharacterArcsPanel.tsx` | 角色弧线追踪 | GET `/api/books/:id/arcs` |
| CheckpointPanel | `CheckpointPanel.tsx` | 快照列表 + 回滚预览 | GET/POST `/api/books/:id/checkpoints/...` |
| CockpitOverview | `CockpitOverview.tsx` | 驾驶舱总览 + 新书引导入口 | GET `/api/books/:id/cockpit` |
| CompliancePanel | `CompliancePanel.tsx` | 敏感词扫描 + AI 声明 | POST `/api/compliance/...` |
| DailyProgressCard | `DailyProgressCard.tsx` | 日更进度追踪 | GET `/api/books/:id/progress/daily` |
| ExportPanel | `ExportPanel.tsx` | 导出 TXT/Word/ePub | POST `/api/books/:id/export` |
| InlineWritePanel | `InlineWritePanel.tsx` | 续写/扩写/补写 | POST `/api/books/:id/inline-write` |
| JingweiEntryEditor | `JingweiEntryEditor.tsx` | 经纬条目编辑 | PATCH `/api/books/:id/jingwei/entries/:eid` |
| NewBookGuide | `NewBookGuide.tsx` | 11 题三模式新书引导 | POST `/api/onboarding/...` |
| PresetsPanel | `PresetsPanel.tsx` | 流派/文风/基底预设管理 | GET/POST `/api/presets/...` |
| PresetSuggestionCard | `PresetSuggestionCard.tsx` | 预设推荐卡片 | 纯前端 |
| StyleDriftPanel | `StyleDriftPanel.tsx` | 文风漂移检测 | POST `/api/books/:id/chapters/:ch/tone-check` |
| TemplateMarketPanel | `TemplateMarketPanel.tsx` | 模板市场浏览与应用 | GET `/api/presets/bundles` |
| VariantsPanel | `VariantsPanel.tsx` | 多版本变体生成 | POST `/api/books/:id/variants` |
| WritingToolsPanel | `WritingToolsPanel.tsx` | 写作工具入口面板 | 各工具独立 API |

---

## 状态管理

NovelFork 前端采用 **局部状态 + props 下钻** 模式，不使用全局状态管理库：

| 层级 | 方式 | 说明 |
|------|------|------|
| 路由级 | `useState` in `WritingWorkbenchRoute` | viewMode, showToolsPanel, showCheckpoints, showJingwei |
| 画布级 | `useState` in `WorkbenchCanvas` | content, dirty, saving, saveError |
| 面板级 | `useState` in 各 Panel | 各面板独立的 loading/result/error |
| 跨组件 | props + callbacks | onSave, onOpen, onCanvasContextChange |
| 后端同步 | `useEffect` + fetch | 按需加载，无 SWR/React Query |

### 画布上下文广播

`WorkbenchCanvas` 通过 `onCanvasContextChange` 回调向上层广播当前画布状态：

```typescript
interface WorkbenchCanvasContext {
  activeResourceId: string | null;
  activeKind: WorkbenchResourceKind | null;
  activeTabId?: string;
  activeResource?: WorkspaceResourceRef;
  openTabs: OpenResourceTab[];
  dirty: boolean;
  contentPreview: string; // 前 500 字符
}
```

此上下文被注入到叙述者会话的 system prompt 中，使 Agent 感知用户当前正在编辑的资源。

---

## 关键 Hooks

| Hook | 文件 | 职责 |
|------|------|------|
| `useWorkbenchResources` | `useWorkbenchResources.ts` | 将后端 ContractResourceNode 转换为前端 WorkbenchResourceNode 树，计算 capabilities |
| `useJingweiEntries` | `jingwei/hooks/useJingweiEntries.ts` | 加载经纬条目列表，支持按分类过滤 |
| `useJingweiProgressions` | `jingwei/hooks/useJingweiProgressions.ts` | 加载经纬进度数据 |

### useWorkbenchResources 详解

```typescript
// 输入: 后端返回的 ContractResourceNode[]
// 输出: { tree, resourceMap, openableNodes, errors }
export function useWorkbenchResources(nodes: readonly ContractResourceNode[]) {
  return useMemo(() => {
    const tree = buildWorkbenchResourceTree(nodes);
    const resourceMap = flattenWorkbenchResourceTree(tree);
    const openableNodes = Array.from(resourceMap.values())
      .filter(node => node.capabilities.open);
    return { tree, resourceMap, openableNodes, errors: [] };
  }, [nodes]);
}
```

核心转换逻辑：
1. `ContractResourceCapabilities` → `WorkbenchResourceCapabilities`（open/readonly/edit/delete/apply）
2. 递归映射子节点
3. 扁平化为 Map 供快速查找

---

## 资源类型映射

| kind | 中文标签 | 视图组件 | 可编辑 |
|------|---------|---------|--------|
| chapter | 章节 | ChapterEditor | 是 |
| candidate | 候选稿 | ChapterEditor + CandidateActionsBar | 是 |
| draft | 草稿 | ChapterEditor | 是 |
| story | 大纲与设定 | MarkdownViewer | 只读 |
| jingwei | 经纬资料 | MarkdownViewer | 只读 |
| jingwei-section | 经纬分区 | BibleCategoryView | 只读 |
| jingwei-entry / bible-entry | 经纬条目 | JingweiEntryEditor | 是 |
| narrative-line / storyline | 叙事线 | NarrativeLineView | 只读 |
| tool-result | 工具结果 | ToolResultView | 只读 |

---

## 经纬子系统前端

`jingwei/` 子目录包含经纬资料管理的完整前端：

```
jingwei/
├── category-schemas.ts    — 分类字段定义（角色/地点/物品/势力/事件等）
├── genre-templates.ts     — 题材模板（玄幻/都市/科幻等预设分类）
├── JingweiCategorySidebar.tsx — 左侧分类导航
├── JingweiEntryForm.tsx   — 条目编辑表单（动态字段）
├── JingweiEntryList.tsx   — 条目列表视图
├── JingweiEntryTree.tsx   — 条目树视图（支持嵌套）
├── JingweiGraphView.tsx   — 关系图可视化
├── JingweiPanel.tsx       — 顶层面板容器
├── JingweiProgressions.tsx — 进度追踪视图
└── hooks/
    ├── useJingweiEntries.ts      — 条目数据加载
    └── useJingweiProgressions.ts — 进度数据加载
```
