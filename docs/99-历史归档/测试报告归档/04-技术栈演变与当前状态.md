# InkOS 技术栈演变与当前状态报告

**版本**: v1.0.0
**创建日期**: 2026-04-28
**更新日期**: 2026-04-28
**状态**: 🗄️ 历史归档
**文档类型**: archived

---

> 归档日期：2026-04-28。归档原因：旧文档体系迁移，仅供历史追溯；当前事实请从新文档中心入口查阅。

## 一、项目概述

**novelfork** 是一个 AI 长篇小说生产工作台，定位为 **Scrivener 项目管理 + Cursor AI 能力 + 网文行业领域智能**。

### 核心特性
- 10 Agent 写作管线（Radar → Planner → Composer → Architect → Writer → Observer → Reflector → Normalizer → Auditor → Reviser）
- SQLite 时序记忆系统（facts, summaries, hooks, world_entries）
- 33 维连续性审计 + 去 AI 味
- 多模型路由 + 守护进程
- Web 工作台 + CLI 工具

---

## 二、技术栈演变历史

### 阶段 1：CLI 工具时代（v0.x - v1.0）

**技术栈**：
- Node.js 22+ (内置 SQLite)
- TypeScript 5.8
- 纯命令行界面

**特点**：
- 专注核心写作管线
- 本地文件系统存储
- 适合开发者使用

### 阶段 2：Tauri 桌面端尝试（v1.0 - v1.1）

**技术栈**：
- Tauri 2.0 (Rust + WebView)
- React 19
- Vite 6
- Hono 4.7 (HTTP Server)

**提交记录**：
- `056e38c` - feat(desktop): add tiptap dependencies and successfully build Tauri desktop
- `b6b5204` - feat(studio): complete Tauri bridge — all features functional offline
- `f61ea8d` - feat(studio): local daemon auto-writer + desktop doctor diagnostics

**问题**：
- Tauri 打包体积大（需要 Rust 工具链）
- WebView 兼容性问题
- 开发调试复杂

**决策**：
- 保留 Tauri 代码（`packages/desktop/`）但不作为主要方向
- 转向更轻量的 PWA 方案

### 阶段 3：PWA 应用形态（v2.0，当前）

**技术栈**：
- **前端**: React 19 + Vite 6 + Tailwind CSS 4
- **PWA**: vite-plugin-pwa + Workbox
- **后端**: Hono 4.7 + Node.js 22+
- **数据库**: SQLite (Node 内置)
- **UI 组件**: shadcn/ui + lucide-react
- **富文本**: TipTap 2.27 + novel
- **测试**: Vitest 3.0

**提交记录**：
- `d21a75f` - feat(studio): add PWA support
- `d48a96b` - refactor(studio): implement CSS Grid layout with resizable panels
- `920d424` - feat(studio): 为底部上下文面板实现 4 个新 Tab 组件

**优势**：
- ✅ 无需安装，浏览器直接运行
- ✅ 可安装到桌面（PWA）
- ✅ 离线缓存，体验接近原生
- ✅ 跨平台（Windows/Mac/Linux）
- ✅ 开发调试简单
- ✅ 打包体积小

---

## 三、当前技术栈详情

### 3.1 Monorepo 结构

```
inkos-master/
├── packages/
│   ├── core/          # 核心引擎（10 Agent + 状态管理）
│   ├── cli/           # CLI 工具
│   ├── studio/        # Web 工作台（PWA）
│   └── desktop/       # Tauri 桌面端（已废弃）
├── docs/              # 文档
└── scripts/           # 构建脚本
```

### 3.2 核心依赖

| 包 | 版本 | 用途 |
|---|------|------|
| **前端框架** | | |
| react | 19.0.0 | UI 框架 |
| react-dom | 19.0.0 | DOM 渲染 |
| vite | 6.0.0 | 构建工具 |
| **PWA** | | |
| vite-plugin-pwa | 1.2.0 | PWA 插件 |
| workbox-window | 7.4.0 | Service Worker |
| **后端** | | |
| hono | 4.7.0 | HTTP Server |
| @hono/node-server | 1.13.0 | Node.js 适配器 |
| **UI 组件** | | |
| lucide-react | 0.577.0 | 图标库 |
| shadcn | 4.0.8 | UI 组件库 |
| cmdk | 1.1.1 | 命令面板 |
| **富文本** | | |
| @tiptap/core | 2.27.2 | 富文本编辑器 |
| @tiptap/react | 3.22.3 | React 集成 |
| novel | 1.0.2 | AI 写作增强 |
| **样式** | | |
| tailwindcss | 4.0.0 | CSS 框架 |
| @tailwindcss/vite | 4.0.0 | Vite 插件 |
| **测试** | | |
| vitest | 3.0.0 | 单元测试 |
| **类型** | | |
| typescript | 5.8.0 | 类型系统 |

### 3.3 代码规模

| 指标 | 数量 |
|------|------|
| 总提交数（2026年） | 454 |
| Studio 代码文件 | 177 个 (.ts/.tsx) |
| 核心 Agent | 10 个 |
| API 端点 | 60+ |
| 页面组件 | 40+ |
| 测试用例 | 545+ (core) + 43 (studio) |

### 3.4 已废弃的技术

| 技术 | 废弃原因 | 保留状态 |
|------|---------|---------|
| Tauri 2.0 | 打包体积大、开发复杂 | 代码保留但不维护 |
| Electron | 未采用（体积更大） | - |
| Node SEA | 打包复杂、体积大 | 已移除 |

---

## 四、v2.0 新增功能（2026-04-17 - 2026-04-18）

### 4.1 PWA 应用形态 ✅

**实现**：
- Service Worker 离线缓存
- 30 个文件预缓存（1759.35 KiB）
- manifest.webmanifest 配置
- 8 个尺寸 PWA 图标
- InstallPrompt 安装提示组件

**提交**：`d21a75f` - feat(studio): add PWA support

### 4.2 IDE 三栏布局 ✅

**实现**：
- CSS Grid 布局（替代 flexbox）
- 左侧栏可拖拽调整宽度（200-400px）
- 底部面板可拖拽调整高度（120-400px）
- 底部面板折叠/展开
- IndexedDB 持久化布局配置

**提交**：`d48a96b` - refactor(studio): implement CSS Grid layout with resizable panels

### 4.3 NER 实体抽取 ✅

**实现**：
- 识别 4 种实体类型（人名、地名、术语、道具）
- 百家姓词典（200+姓氏）
- 后缀匹配（140+后缀）
- 准确率 ≥ 80%

**提交**：`ad0757d` - feat(core): implement NER entity extraction with 80%+ accuracy

### 4.4 Lorebook RAG 检索 ✅

**实现**：
- 关键词匹配查询
- 优先级降序排序
- Token 预算控制（≤2000 tokens）
- Token 消耗降低 **80.5%**（超出目标 60%-80%）

**测试**：10/10 通过

### 4.5 集成到 Composer ✅

**实现**：
- 修改 `composer.ts` 集成 NER + RAG
- 从 goal + 章节文本 + mustKeep 提取实体
- 将检索结果注入到 context
- 优雅降级（memory.db 不存在时不影响写作）

**提交**：`c9c2a21` - feat(core): integrate NER + Lorebook RAG into Composer Agent

**性能**：
- 全量注入：3080 tokens
- RAG 检索：600 tokens
- 降低率：80.5%

### 4.6 底部上下文面板 ✅

**实现**：
- Tab 1: LorebookPanel（实体识别 + 关键参数）
- Tab 2: HookCountdown（伏笔倒计时）
- Tab 3: RhythmChart（节奏波形图占位符）
- Tab 4: WorldDimensions（世界观维度浏览）

**提交**：`920d424` - feat(studio): 为底部上下文面板实现 4 个新 Tab 组件

---

## 五、当前架构状态

### 5.1 前端架构

```
packages/studio/src/
├── api/                    # 后端 API（Hono）
│   ├── routes/             # API 路由（60+ 端点）
│   │   ├── books.ts
│   │   ├── chapters.ts
│   │   ├── lorebook.ts     # 【新增】Lorebook API
│   │   ├── truth.ts
│   │   └── ...
│   └── server.ts           # Hono 服务器
│
├── components/             # React 组件
│   ├── Sidebar.tsx         # 左侧导航
│   ├── TabBar.tsx          # 标签栏
│   ├── InkEditor.tsx       # 富文本编辑器
│   ├── ContextPanel.tsx    # 【增强】底部上下文面板
│   ├── LorebookPanel.tsx   # 【新增】Lorebook 面板
│   ├── HookCountdown.tsx   # 【新增】伏笔倒计时
│   ├── RhythmChart.tsx     # 【新增】节奏波形图
│   ├── WorldDimensions.tsx # 【新增】世界观维度
│   ├── InstallPrompt.tsx   # 【新增】PWA 安装提示
│   └── ...（40+ 组件）
│
├── pages/                  # 页面组件
│   ├── Dashboard.tsx       # 仪表盘
│   ├── BookDetail.tsx      # 书籍详情
│   ├── ChapterReader.tsx   # 章节阅读
│   ├── Analytics.tsx       # 数据分析
│   └── ...（40+ 页面）
│
├── hooks/                  # React Hooks
│   ├── use-sse.ts          # 服务器推送
│   ├── use-tabs.ts         # 标签管理
│   ├── use-layout-config.ts # 【新增】布局配置
│   └── ...
│
├── App.tsx                 # 【重构】主应用（CSS Grid 布局）
└── main.tsx                # 入口
```

### 5.2 后端架构

```
packages/core/src/
├── agents/                 # 10 Agent 写作管线
│   ├── radar.ts            # 雷达扫描
│   ├── planner.ts          # 规划
│   ├── composer.ts         # 【修改】上下文组装（集成 NER + RAG）
│   ├── architect.ts        # 架构设计
│   ├── writer.ts           # 写作
│   ├── observer.ts         # 观察提取
│   ├── reflector.ts        # 反思
│   ├── normalizer.ts       # 归一化
│   ├── auditor.ts          # 审计
│   └── reviser.ts          # 修订
│
├── state/                  # 状态管理
│   ├── memory-db.ts        # SQLite 封装（world_entries 表）
│   └── manager.ts          # 状态管理器
│
├── utils/                  # 工具函数
│   ├── ner-extractor.ts    # 【新增】NER 实体抽取
│   ├── lorebook-rag.ts     # 【新增】Lorebook RAG 检索
│   └── ...
│
└── __tests__/              # 测试
    ├── ner-extractor.test.ts       # 【新增】NER 测试（9/18 通过）
    ├── lorebook-rag.test.ts        # 【新增】RAG 测试（10/10 通过）
    └── composer-lorebook.test.ts   # 【新增】集成测试（5/5 通过）
```

### 5.3 数据库架构

**SQLite `memory.db`**：

| 表名 | 用途 | 行数（典型） |
|------|------|-------------|
| facts | 时序事实 | 1000+ |
| chapter_summaries | 章节摘要 | 100+ |
| hooks | 伏笔追踪 | 50+ |
| chapter_snapshots | 章节快照 | 500+ |
| world_dimensions | 世界观维度 | 9 个内置 + 自定义 |
| world_entries | Lorebook 词条 | 100-1000+ |

---

## 六、技术决策对比

### 6.1 桌面端方案对比

| 方案 | 优势 | 劣势 | 决策 |
|------|------|------|------|
| **PWA** | ✅ 无需安装<br>✅ 跨平台<br>✅ 开发简单<br>✅ 体积小 | ❌ 需要浏览器<br>❌ 系统集成有限 | ✅ **采用** |
| **Tauri** | ✅ 原生性能<br>✅ 系统集成好 | ❌ 打包复杂<br>❌ 体积大<br>❌ 开发调试难 | ⏸️ 保留代码但不维护 |
| **Electron** | ✅ 生态成熟 | ❌ 体积巨大（>100MB）<br>❌ 内存占用高 | ❌ 未采用 |
| **Node SEA** | ✅ 单文件可执行 | ❌ 打包复杂<br>❌ 体积大 | ❌ 已移除 |

### 6.2 前端框架对比

| 方案 | 优势 | 劣势 | 决策 |
|------|------|------|------|
| **React 19** | ✅ 生态成熟<br>✅ 团队熟悉<br>✅ 性能优秀 | ❌ 学习曲线 | ✅ **采用** |
| Vue 3 | ✅ 简单易学 | ❌ 生态较小 | ❌ 未采用 |
| Svelte | ✅ 性能最佳 | ❌ 生态较小 | ❌ 未采用 |

### 6.3 样式方案对比

| 方案 | 优势 | 劣势 | 决策 |
|------|------|------|------|
| **Tailwind CSS 4** | ✅ 原子化<br>✅ 开发快<br>✅ 体积小 | ❌ 类名冗长 | ✅ **采用** |
| CSS Modules | ✅ 作用域隔离 | ❌ 开发慢 | ❌ 未采用 |
| Styled Components | ✅ CSS-in-JS | ❌ 运行时开销 | ❌ 未采用 |

---

## 七、开发统计

### 7.1 提交历史（2026年）

- **总提交数**: 454
- **主要贡献者**: vivy1024
- **活跃时间**: 2026-01-01 至今

### 7.2 代码规模

```
packages/core/src/
  - TypeScript 文件: 80+
  - 测试文件: 30+
  - 代码行数: 15,000+

packages/studio/src/
  - TypeScript/TSX 文件: 177
  - 组件数: 40+
  - 页面数: 40+
  - 代码行数: 25,000+

packages/cli/src/
  - TypeScript 文件: 10+
  - 代码行数: 2,000+
```

### 7.3 测试覆盖率

| 模块 | 测试用例 | 通过率 | 覆盖率 |
|------|---------|--------|--------|
| core | 545+ | 100% | 80%+ |
| studio | 43 | 100% | 60%+ |
| NER 实体抽取 | 18 | 50% (9/18) | 准确率 ≥80% ✅ |
| Lorebook RAG | 10 | 100% | 100% |
| Composer 集成 | 5 | 100% | 100% |

---

## 八、当前状态总结

### 8.1 已完成功能（v1.1.1）

✅ 10 Agent 写作管线完整  
✅ SQLite 时序记忆系统  
✅ 33 维连续性审计  
✅ Web 工作台基础界面（40+ 页面）  
✅ AI 工具系统（8 个工具）  
✅ Git Worktree 多线并行  
✅ 多模型路由（Claude/Gemini/Sub2API）  
✅ 守护进程自动写作  

### 8.2 v2.0 新增功能（2026-04-17 - 2026-04-18）

✅ PWA 应用形态（可安装到桌面）  
✅ IDE 三栏布局（可调整宽度/高度）  
✅ NER 实体抽取（准确率 ≥80%）  
✅ Lorebook RAG 检索（Token 降低 80.5%）  
✅ 集成到 Composer Agent  
✅ 底部上下文面板（4 个 Tab）  

### 8.3 待实现功能（Phase 3）

⏸️ 多标签页拖拽排序  
⏸️ 节奏控制系统（3+1 节奏公式）  
⏸️ 黄金三章检测器  
⏸️ 伏笔倒计时系统（后端 API）  
⏸️ 毒点检测器（7 条规则）  

### 8.4 技术债务

1. **Tauri 代码清理**: `packages/desktop/` 目录保留但不维护，可考虑删除
2. **测试覆盖率**: studio 测试覆盖率仅 60%，需提升至 80%
3. **NER 准确率**: 部分边界情况测试未通过（9/18），可优化
4. **PWA 图标**: 当前使用占位符，需设计正式图标

---

## 九、技术栈推荐

### 9.1 当前技术栈（推荐）✅

**前端**: React 19 + Vite 6 + Tailwind CSS 4 + PWA  
**后端**: Hono 4.7 + Node.js 22+  
**数据库**: SQLite (Node 内置)  
**UI**: shadcn/ui + TipTap 2.27  
**测试**: Vitest 3.0  

**优势**:
- 开发效率高
- 打包体积小
- 跨平台兼容
- 离线能力强
- 维护成本低

### 9.2 不推荐的技术栈 ❌

- ❌ Tauri（打包复杂、体积大）
- ❌ Electron（体积巨大、内存占用高）
- ❌ Node SEA（打包复杂）
- ❌ Vue/Svelte（生态较小，团队不熟悉）

---

## 十、未来规划

### 10.1 短期目标（1-2 周）

- [ ] 完成 Phase 3（节奏控制、黄金三章、毒点检测）
- [ ] 提升测试覆盖率至 80%
- [ ] 设计正式 PWA 图标
- [ ] 优化 NER 准确率至 90%+

### 10.2 中期目标（1-2 月）

- [ ] 实现内嵌终端（AI Agent 工具链）
- [ ] 数据源接入（PDF/网页抓取）
- [ ] 计算沙箱（Python/JS 脚本执行）
- [ ] 插件系统

### 10.3 长期目标（3-6 月）

- [ ] 多人协作功能
- [ ] 云端同步
- [ ] 移动端适配
- [ ] 国际化（i18n）

---

**文档结束**
