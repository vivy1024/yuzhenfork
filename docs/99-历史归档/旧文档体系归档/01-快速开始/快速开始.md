# NovelFork 快速开始指南

**版本**: v1.0.0
**创建日期**: 2026-04-28
**更新日期**: 2026-04-28
**状态**: 🗄️ 历史归档
**文档类型**: archived

---

> 归档日期：2026-04-28。归档原因：旧文档体系迁移，仅供历史追溯；当前事实请从新文档中心入口查阅。

## 📋 概述

这份快速开始描述的是当前仓库的源码运行方式，用于开发和过渡期验证。它不是最终产品交付形态；最终目标仍是 NarraFork 风格的本地单体应用。

`★ 核心价值 ─────────────────────────────────────`
NovelFork 通过多 Agent 协作管线，实现了零人工干预的章节创作。内置 33 维度连续性审计、文风仿写、去 AI 味等功能，支持玄幻、仙侠、都市、科幻等题材。
`─────────────────────────────────────────────────`

---

## 🚀 5分钟快速体验

### 前置条件

```bash
# ✅ 1. Node.js 环境（必需）
node --version          # ≥20.0.0
pnpm --version          # ≥9.0.0

# ✅ 2. Git 环境（必需，用于 Worktree 多线并行）
git --version           # ≥2.30

# ✅ 3. 基础硬件要求
#    - 内存：≥8GB（推荐16GB）
#    - 磁盘：≥10GB可用空间
#    - CPU：≥4核心
```

### 步骤1：安装 NovelFork

```bash
# 从源码安装
git clone https://github.com/vivy1024/novelfork.git novelfork
cd novelfork
pnpm install
pnpm build
npm link packages/cli
```

**预期输出**：
```
✓ Built packages/core
✓ Built packages/cli
✓ Built packages/studio
✓ Linked novelfork command
```

### 步骤2：配置 LLM 提供商

> 如果你当前只想先体验本地工作流，这一步可以暂时跳过。未配置模型时，仍可创建本地书籍、整理故事经纬、编辑章节；只有在触发 AI 续写、评点、深度 AI 味检测或工作台 Agent 时，系统才会提示配置模型。

```bash
# 全局配置（推荐，只需一次）
novelfork config set-global \
  --provider openai \
  --base-url https://api.openai.com/v1 \
  --api-key sk-xxx \
  --model gpt-4o
```

**预期输出**：
```json
{
  "success": true,
  "message": "全局配置已保存到 ~/.novelfork/.env"
}
```

### 步骤3：创建第一本书

```bash
# 创建新书
novelfork book create \
  --title "吞天魔帝" \
  --genre xuanhuan \
  --chapter-words 3000 \
  --target-chapters 100
```

**预期输出**：
```
✓ 书籍创建成功
  ID: book_001
  标题: 吞天魔帝
  题材: 玄幻
  目标章节: 100章
  每章字数: 3000字
```

### 步骤4：写第一章

```bash
# 完整管线写作（草稿 → 审计 → 修订）
novelfork write next 吞天魔帝
```

**预期输出**：
```
[1/8] 雷达扫描平台趋势...
[2/8] 规划章节意图...
[3/8] 编排上下文...
[4/8] 建筑师规划结构...
[5/8] 写手生成正文...
[6/8] 观察者提取事实...
[7/8] 连续性审计...
[8/8] 修订优化...

✓ 第1章创作完成
  字数: 3,124字
  审计通过率: 100%
  耗时: 2分15秒
```

## 🧭 Studio 首次打开建议

1. 推荐优先配置 AI 模型，但可以跳过。
2. 未配置模型时，可先完成：创建本地书籍、选择故事经纬结构、手工维护经纬栏目、编辑章节正文。
3. 如果你已经有现成栏目目录，现在也可以在故事经纬的栏目管理里使用 **Markdown 目录导入** 快速建立栏目。
4. 《没钱修什么仙》只作为高级范本导入样例，不是默认标准；栏目结构应以当前作品需求为准。

### 步骤5：查看创作结果

```bash
# 查看书籍状态
novelfork status 吞天魔帝

# 导出全书
novelfork export 吞天魔帝 --format txt
```

---

## 🎯 三种使用方式

### 方式1：完整管线（推荐新手）

```bash
# 一键式写作（草稿 → 审计 → 修订）
novelfork write next 吞天魔帝

# 连续写5章
novelfork write next 吞天魔帝 --count 5

# 指定字数
novelfork write next 吞天魔帝 --words 5000
```

**适用场景**：
- ✅ 零人工干预的自动化创作
- ✅ 保证质量的批量生产
- ✅ 守护进程后台运行

### 方式2：原子命令（可组合）

```bash
# 规划章节意图
novelfork plan chapter 吞天魔帝 --context "本章重点写师徒矛盾"

# 编排上下文
novelfork compose chapter 吞天魔帝

# 只写草稿
novelfork draft 吞天魔帝

# 审计指定章节
novelfork audit 吞天魔帝 1

# 修订指定章节
novelfork revise 吞天魔帝 1
```

**适用场景**：
- ✅ 精细控制创作流程
- ✅ 调试和优化
- ✅ 自定义工作流

### 方式3：自然语言 Agent 模式

```bash
# 自然语言指令
novelfork agent "帮我写一本都市修仙，主角是个程序员"

# 复杂任务
novelfork agent "先扫描市场趋势，然后根据结果创建一本新书"

# 创作指导
novelfork agent "写下一章，重点写师徒矛盾，字数3000字"
```

**适用场景**：
- ✅ 快速原型验证
- ✅ 复杂任务编排
- ✅ 自然交互体验

---

## 🔧 验证核心功能

### 1. 验证多 Agent 管线

```bash
# 查看管线执行日志
novelfork write next 吞天魔帝 --json | jq '.pipeline'
```

**预期输出**：
```json
{
  "stages": [
    {"name": "radar", "status": "completed", "duration_ms": 1234},
    {"name": "planner", "status": "completed", "duration_ms": 2345},
    {"name": "composer", "status": "completed", "duration_ms": 3456},
    {"name": "architect", "status": "completed", "duration_ms": 4567},
    {"name": "writer", "status": "completed", "duration_ms": 5678},
    {"name": "observer", "status": "completed", "duration_ms": 6789},
    {"name": "auditor", "status": "completed", "duration_ms": 7890},
    {"name": "reviser", "status": "completed", "duration_ms": 8901}
  ]
}
```

### 2. 验证连续性审计

```bash
# 查看审计报告
novelfork audit 吞天魔帝 1 --json | jq '.audit_report'
```

**预期输出**：
```json
{
  "dimensions": 33,
  "passed": 33,
  "failed": 0,
  "critical_issues": [],
  "warnings": [],
  "score": 100
}
```

### 3. 验证长期记忆

```bash
# 查看真相文件
ls story/state/

# 预期输出
current_state.json
particle_ledger.json
pending_hooks.json
chapter_summaries.json
subplot_board.json
emotional_arcs.json
character_matrix.json
```

### 4. 验证 Web 工作台

```bash
# 启动 Studio
novelfork studio

# 浏览器访问
open http://localhost:4567
```

---

## 🏗️ 项目结构概览

```
novelfork/
├── packages/
│   ├── core/                    # 核心逻辑
│   │   ├── agents/             # 8个Agent实现
│   │   ├── memory/             # 长期记忆系统
│   │   ├── audit/              # 33维度审计
│   │   └── llm/                # LLM提供商适配
│   │
│   ├── cli/                     # 命令行工具
│   │   ├── commands/           # CLI命令
│   │   └── utils/              # 工具函数
│   │
│   └── studio/                  # Web工作台
│       ├── src/api/            # Hono后端
│       ├── src/components/     # React组件
│       └── src/stores/         # 状态管理
│
└── story/                       # 书籍数据（自动生成）
    ├── state/                  # 7个真相文件（JSON）
    ├── runtime/                # 运行时产物
    ├── chapters/               # 章节正文
    └── memory.db               # SQLite时序记忆
```

### 核心特性

- **多 Agent 管线**: 8个专业 Agent 接力完成章节创作
- **连续性审计**: 33维度检查，确保逻辑自洽
- **长期记忆**: 7个真相文件 + SQLite 时序数据库
- **去 AI 味**: 内置词汇疲劳词表、禁用句式、文风指纹
- **文风仿写**: 提取统计指纹，注入创作流程
- **多模型路由**: 不同 Agent 使用不同模型
- **Web 工作台**: 可视化管理、实时监控、数据分析

---

## 📊 性能指标

| 指标 | 数值 | 说明 |
|------|------|------|
| **章节创作时间** | 2-5分钟 | 取决于模型和字数 |
| **审计通过率** | >95% | 33维度检查 |
| **平均章字数** | 3000-15000字 | 可配置 |
| **并发能力** | 10章/小时 | 守护进程模式 |
| **内存占用** | <500MB | 单书项目 |
| **磁盘占用** | ~100MB/书 | 包含所有数据 |

---

## 🎉 成功标志

当你看到以下标志时，说明 NovelFork 已成功运行：

```
✅ NovelFork CLI 已安装 (novelfork --version)
✅ 全局配置已保存 (~/.novelfork/.env)
✅ 书籍创建成功 (story/ 目录已生成)
✅ 第一章创作完成 (story/chapters/chapter-0001.md)
✅ 真相文件已生成 (story/state/*.json)
✅ 审计通过率 100% (无关键问题)
✅ Web 工作台可访问 (http://localhost:4567)

🎊 NovelFork 启动成功！可以开始创作你的网文小说！
```

---

## 🔗 下一步探索

### 📚 学习资源

- 安装指南（原链接：`./安装指南.md`） - 详细安装步骤
- 环境配置（原链接：`./环境配置.md`） - 配置说明
- 系统架构（原链接：`../02-核心架构/01-系统架构/01-系统架构总览.md`） - 了解技术细节
- 开发指南（原链接：`../02-开发指南/README.md`） - 开发者指南

### 🚀 进阶功能

1. **文风仿写**：`novelfork style analyze` 提取参考文本风格
2. **同人创作**：`novelfork fanfic init` 从原作素材创建同人书
3. **守护进程**：`novelfork up` 后台自动写作
4. **多模型路由**：`novelfork config set-model` 为不同 Agent 配置不同模型
5. **Web 工作台**：`novelfork studio` 可视化管理

### 🛠️ 高级用法

- 创作简报：`--brief my-ideas.md` 传入你的脑洞
- 输入治理：编辑 `story/author_intent.md` 和 `story/current_focus.md`
- 字数治理：`--words` 指定目标字数
- 续写已有作品：`novelfork import chapters` 导入现有章节
- 数据分析：`novelfork analytics` 查看创作统计

---

## ⚠️ 常见问题

### Q: 安装失败怎么办？

```bash
# 检查 Node.js 版本
node --version  # 需要 ≥20.0.0

# 检查 pnpm 版本
pnpm --version  # 需要 ≥9.0.0

# 清理缓存重新安装
rm -rf node_modules
pnpm install
```

### Q: API 调用失败？

```bash
# 诊断配置问题
novelfork doctor

# 检查全局配置
novelfork config show-global

# 测试 API 连通性
curl -X POST https://api.openai.com/v1/chat/completions \
  -H "Authorization: Bearer $NOVELFORK_LLM_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"gpt-4o","messages":[{"role":"user","content":"test"}]}'
```

### Q: 审计不通过怎么办？

```bash
# 查看审计报告
novelfork audit 吞天魔帝 1 --json

# 手动修订
novelfork revise 吞天魔帝 1

# 重写章节
novelfork write rewrite 吞天魔帝 1
```

### Q: 如何提高创作质量？

1. **使用更好的模型**：Claude Opus、GPT-4o
2. **提供创作简报**：`--brief` 传入详细设定
3. **编辑控制文件**：`story/author_intent.md`、`story/current_focus.md`
4. **导入文风指纹**：`novelfork style import`
5. **多模型路由**：写手用 Claude，审计用 GPT-4o

---

**维护者**: 薛小川 (vivy1024)  
**最后更新**: 2026-04-18  
**版本**: v0.0.1  
**文档状态**: 过渡期参考

---

<div align="center">
<strong>🏗️ 多 Agent 协作 · 连续性审计 · 去 AI 味 · 文风仿写 🎯</strong>
<br>
<strong>⚡ 零人工干预 · 📊 质量保障 · 🔌 多模型路由 · 🎯 中文网文专精</strong>
</div>
