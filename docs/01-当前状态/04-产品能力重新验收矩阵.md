# 产品能力重新验收矩阵

**版本**: v1.0.0  
**创建日期**: 2026-05-07  
**更新日期**: 2026-05-07  
**状态**: ✅ 当前有效  
**文档类型**: current

---

## 1. 文档目的

本矩阵用于执行 `claude-codex-novel-agent-v1` 的 Phase 0 Task 1：重新核对 NovelFork 原始产品能力承诺与当前用户可见路径之间的差距。

此前许多 archived specs 已标记完成，但其中不少完成项属于 API、类型、组件、route、测试、合同、接线、退役或口径守卫。它们是有价值的底层资产，但不能自动等同于当前 Studio、CLI 或 headless 已经完成端到端功能。

本矩阵只回答一个问题：

> 用户今天能否在当前 NovelFork 主路径中真实完成这项能力？

---

## 2. 状态口径

| 状态 | 含义 | 是否可宣传为当前完整功能 |
|---|---|---|
| `current` | 已有 Studio 或 CLI/headless 端到端证据，且结果持久化、可恢复、失败透明 | 可以，但必须附证据 |
| `partial` | 有真实底座或部分路径，但未覆盖完整产品闭环 | 不可以 |
| `not-wired` | core/API/component 存在，但没有接入当前主路径或用户操作不自然 | 不可以 |
| `planned` | 已明确规划但当前未实现 | 不可以 |
| `unsupported` | 当前明确不支持，UI/API 应透明说明 | 不可以 |
| `non-goal` | 明确不做或不在本版本目标内 | 不可以 |

### 证据规则

- API 测试只能证明 route 存在。
- 组件测试只能证明 UI 可渲染。
- 单元测试只能证明内部函数行为。
- 文档、能力矩阵、spec checkbox 只能证明历史记录。
- **只有 Studio 浏览器路径或 CLI/headless 路径跑通，才能作为功能完成证据。**

---

## 3. 重新验收总览

| 能力组 | 代表能力 | 当前重验状态 | 结论 |
|---|---|---|---|
| ClaudeCodeCLI 级会话 | resume / fork / compact / transcript / usage | `partial` | 有会话底座和部分 UI/CLI 能力，但还不是统一 ClaudeCodeCLI-class 产品体验 |
| ClaudeCodeCLI 级命令权限 | slash / permission mode / tool allow-deny-ask | `partial` | 有简化 slash 和 toolPolicy，未统一到完整 command/capability registry |
| CodexCLI 级 headless | exec / stream-json / approval / sandbox 语义 | `partial` | 有 headless chat/exec，Codex event taxonomy、sandbox、review、image input 仍非 current |
| 设置页控制面 | 模型、权限、工具、MCP、sandbox、subagent | `partial` | 页面存在且部分真实接线，但还不能完整控制 runtime |
| 套路页能力面 | commands/tools/skills/hooks/subagents/MCP/workflow | `partial` | 页面存在且可读部分真实列表，但还不是可配置 Agent 能力工作台 |
| 小说叙述者主入口 | `/novel:*` 和自然语言写作任务 | `planned` | 当前还没有统一 Novel command pack |
| `/novel:write-next` 闭环 | context → PGI → Guided Plan → approve → candidate | `partial` | 底层工具资产存在，但当前产品闭环需重新验收和接实 |
| 叙事线剧情上下文 | 伏笔、矛盾、角色弧线、当前状态 | `not-wired` | 有 narrative/cockpit/health 资产，但未成为 Agent 核心上下文面 |
| 故事经纬动态认知层 | 问卷、栏目、CoreShift、PGI | `partial` | Bible/Jingwei 底座强，但当前建书→写作主流程仍未完全产品化 |
| 创作画布产物面 | 章节、候选稿、Guided Plan、diff、checkpoint | `partial` | 章节/候选稿基础可用，Guided Plan/diff/checkpoint 产物面还需接实 |
| 写作模式 | 选段续写/扩写/补写/对话/多版本 | `partial` | core/API/组件资产存在，InlineWritePanel + VariantsPanel 已接入新前端工作台；浏览器端到端验证待完成 |
| 写作工具 | 健康、POV、节奏、矛盾、角色弧线、文风 | `partial` | ChapterHealthCard、BookHealthSummary、CharacterArcsPanel、StyleDriftPanel、DailyProgressCard、BeatProgressBar、CompliancePanel、ExportPanel、TemplateMarketPanel 已接入新前端工作台；浏览器端到端验证待完成 |
| Agent 写作管线 | Explorer→Planner→Writer→Auditor | `partial` | 有 prompt/pipeline 函数，缺可见多 Agent 执行链产品体验 |
| MCP / skills / hooks / subagents | 统一 runtime 扩展治理 | `partial` | Routines 页有入口，真实 runtime 调用和权限治理仍需统一 |
| 端到端验收体系 | Studio/CLI/headless 真实证据 | `planned` | 已建立新 spec 任务，尚未形成统一证据矩阵 |

---

## 4. 详细能力矩阵

| # | 能力 | 原始 spec / 来源 | 当前底层资产 | 当前 Studio 路径 | 当前 CLI/headless 路径 | 当前端到端证据 | 重验状态 | 结论 |
|---:|---|---|---|---|---|---|---|---|
| 1 | 会话 create/resume/fork/compact | `conversation-parity-v1` | session lifecycle service、compact service、SessionCenter、ConversationSurface | `/next/sessions`、`/next/narrators/:sessionId` | `novelfork chat`、headless chat | 有组件、route、Playwright 部分证据 | `partial` | 作为 NovelFork 会话能力可用，但尚未统一为 ClaudeCodeCLI-class 全产品基线 |
| 2 | Transcript / usage / result | `conversation-parity-v1`、`web-agent-runtime-v1` | session history、usage envelope、headless result | 叙述者消息流和状态栏 | headless result / stream-json | 有 service/API/CLI 测试 | `partial` | 需统一为 canonical RuntimeEvent，并覆盖 Studio/CLI/headless 同源回放 |
| 3 | Slash command registry / runtime command executor | `conversation-parity-v1`、`claude-codex-novel-agent-v1` Task10-11 | core `command-registry.ts` / `command-executor.ts` 覆盖 `/help`、`/status`、`/model`、`/permission`、`/tools`、`/mcp`、`/agents`、`/compact`、`/resume`、`/fork` 与 `/novel:*` planned 命令 | 叙述者 composer `/` 建议与 slash 执行走共享 executor | CLI help 读取同一 registry；headless slash prompt 直接执行 command executor | core/studio/cli 聚焦测试与 typecheck 证据 | `partial` | SSoT、command event 与权限 resolver 底座已完成；后续仍需接入真实 tools/MCP/agents handler 与 `/novel:*` 工作流 |
| 4 | Permission mode / tool policy / confirmation envelope | `conversation-parity-v1`、`agent-native-workspace-v1`、`claude-codex-novel-agent-v1` Task12-13 | `resolveSessionToolPolicy()` 统一 permission mode、toolPolicy allow/deny/ask、resource risk、dirty canvas guard；`normalizeToolConfirmationRequest()` 统一 confirmation targetResources/source/checkpoint/operations，tool executor、runtime events、transcript audit 与 provider-visible tool schema 均消费同源结构 | 叙述者状态栏、工具确认卡、dirty canvas guard；ConfirmationGate 展示目标资源、来源、checkpoint、diff、approve/reject | headless/CLI 经同一 runtime turn 继承 resolver，pending confirmation 输出同源 permission_request / exit 2 | `session-tool-policy.test.ts`、`runtime-events.test.ts`、`session-chat-service.test.ts`、`session-headless-chat-service.test.ts`、`ConversationSurface.test.tsx`、executor/runtime 聚焦测试与 typecheck | `partial` | resolver 与 confirmation envelope 底座已完成；后续仍需 settings/routines 可视化管理、Codex sandbox/approval 状态模型和真实 `/novel:*` 写作流验收 |
| 5 | Codex-style `exec` / stream-json / sandbox-approval status | `web-agent-runtime-v1`、`conversation-parity-v1`、`claude-codex-novel-agent-v1` Task14 | `/api/sessions/headless-chat`、`novelfork exec`、NDJSON output；`getCodexRuntimeCapabilityStatuses()` 统一 sandbox planned、approval partial、review/image reference-only | 设置页 runtime facts 显示 Codex sandbox/approval/review/image 状态；无直接主操作入口，仅可在会话/工具中间接体现 | `novelfork exec` result event 输出同源 `runtime_capabilities`；unsupported sandbox 请求降级保存为 planned，不影响工具执行 | `codex-runtime-status.test.ts`、`SettingsTruthModel.test.ts`、`user-config-service.test.ts`、headless/runtime-stream 聚焦测试与 typecheck | `partial` | 可复用底座，需接入 `/novel:write-next` 并稳定小说 workflow 端到端证据；未实现 OS sandbox/review/image input 不得写成 current |
| 6 | Codex sandbox / review / image input | `ui-live-parity-hardening-v1` parity guard | parity matrix / status facts | 设置页只应显示 planned/reference-only | 无 | 无真实实现证据 | `planned` | 不得宣传 current；后续按 sandbox/approval 状态模型推进 |
| 7 | 设置页 Runtime Control Center | `ui-live-parity-hardening-v1`、`v0-1-0-release-readiness` | ProviderSettingsPage、SettingsTruthModel、provider runtime store | `/next/settings` | 间接影响 CLI/headless，未统一验收 | 有浏览器 settings E2E | `partial` | provider/model 部分可信，权限、MCP、sandbox、subagent、headless defaults 需接实 |
| 8 | 套路页 Capability Workbench | `v0-1-0-release-readiness`、NarraFork 参考 | RoutinesNextPage、routines API、MCP registry 部分能力 | `/next/routines` | 未形成同源 CLI/headless registry | 有 routines Playwright 验活 | `partial` | 页面可达，但启用/禁用/配置后影响 runtime 的闭环还未完成 |
| 9 | `/novel:init` | `onboarding-and-story-jingwei`、`novel-bible-v1` | book create、questionnaire、jingwei/bible APIs | 首页/作品创建与部分问卷能力 | CLI 未统一 | 建书路径有 release-readiness E2E，命令无 | `planned` | 需要作为 Novel command 接入 runtime |
| 10 | `/novel:outline` | `writing-modes-v1`、`novel-creation-workbench-complete-flow` | outline brancher、outline editor/API | 工作台大纲节点/branch 资产 | 未统一 | 主要为 API/组件证据 | `not-wired` | 需要作为 command/workflow 进入叙述者和 CLI/headless |
| 11 | `/novel:write-next` | `agent-native-workspace-v1`、`agent-writing-pipeline-v1` | cockpit tools、PGI、guided、candidate tool、Writer agent | 工作台写作动作/叙述者会话 | `novelfork exec` 尚未跑同一 workflow | 旧矩阵声称真实可用，但当前需重验 | `partial` | 底座存在；必须重新跑通 context→PGI→Plan→approve→candidate→canvas |
| 12 | PGI 生成前追问 | `novel-bible-v1`、`agent-native-workspace-v1` | pgi-engine、session tools、metadata | 工具卡/计划流资产 | 未统一 | unit/integration 资产为主 | `partial` | 需要在 `/novel:write-next` 和写作前主流程中强制出现或说明无问题原因 |
| 13 | Guided Generation Plan | `agent-native-workspace-v1` | guided state/tools/renderers | 叙述者工具结果/画布资产 | 未统一 | service/UI 测试为主 | `partial` | 需要让用户在主流程中批准/拒绝/修改，并可刷新恢复 |
| 14 | 候选稿创建/应用 | `novel-creation-workbench-complete-flow`、`agent-native-workspace-v1` | candidate APIs、CandidateEditor、checkpoint/rewind service | 工作台资源树与画布 | headless 遇确认门未完整验收 | 部分 Playwright/route 证据 | `partial` | 基础候选稿可用；需统一 candidate_created event、diff、checkpoint、确认入库流程 |
| 15 | 章节编辑/保存 | `novel-creation-workbench-complete-flow` | 章节 API、WorkbenchCanvas、dirty guard | `/next/books/:bookId` 画布 | 无 CLI 正文编辑需求 | 有资源编辑 E2E | `current` | 可作为当前真实能力，但需纳入 Agent 写入保护链 |
| 16 | 叙事线剧情上下文 | `agent-native-workspace-v1`、`longform-cockpit-v1`、`writing-tools-v1` | narrative-line service、cockpit、health tools | 资源/工具卡/画布组件资产 | 未统一 | 主要为服务/API 测试 | `not-wired` | 未成为 Agent 写作的必读上下文面 |
| 17 | 故事经纬基础 CRUD | `novel-bible-v1` | Bible/Jingwei repos、routes、Workspace views | 经纬资源节点/面板 | 未统一 | route/UI 测试较多 | `partial` | CRUD 底座存在；动态栏目/上下文预览/主流程接线仍需重验 |
| 18 | 问卷 → 经纬 | `novel-bible-v1`、`onboarding-and-story-jingwei` | questionnaire templates/submit/mapping | 建书后问卷资产，当前主路径需核实 | 未统一 | 主要为底层和历史 E2E 记录 | `partial` | 需要在当前建书和 `/novel:init` 中产品化 |
| 19 | CoreShift | `novel-bible-v1` | core shift engine/routes/UI 资产 | 变更历史/影响分析资产 | 无 | unit/route/组件资产为主 | `partial` | 需要接入经纬核心设定编辑和 Agent 写作风险提示 |
| 20 | 选段续写/扩写/补写 | `writing-modes-v1` | core agents、writing modes routes、components | 当前章节编辑器内自然入口需核实 | 无同源 CLI | 历史组件/API 证据为主 | `not-wired` | 必须回到当前画布选区交互，不能只算 API/组件完成 |
| 21 | 对话生成/多版本对比 | `writing-modes-v1` | dialogue/variant agents、components | 当前画布入口需核实 | 无同源 CLI | 历史组件/API 证据为主 | `not-wired` | 需要选区/光标上下文、预览、接受/丢弃闭环 |
| 22 | 写作工具健康视图 | `writing-tools-v1` | health、POV、rhythm、dialogue、conflict、arc、tone tools/components | 当前工作台入口不稳定 | 无 | 多为 core/API/component 证据 | `not-wired` | 需成为叙事线/画布日常面板，缺数据透明降级 |
| 23 | 章节钩子生成 | `writing-tools-v1` | hook generator、pending_hooks 更新资产 | 工具/资源资产 | 未统一 | API/组件/历史流程证据 | `partial` | 需接入 candidate/draft/jingwei 更新和确认门 |
| 24 | Agent 写作管线 | `agent-writing-pipeline-v1` | agent prompts、runWritingPipeline、agent-context | 工作台入口资产 | 未统一 | prompt/context 测试为主 | `partial` | 需要可见 Explorer→Planner→Writer→Auditor 执行链和配置影响 |
| 25 | Subagent 配置 | `agent-writing-pipeline-v1`、`web-agent-runtime-v1` | subagent config、ToolsTab/Settings assets | 设置/套路页入口需接实 | 未统一 | 组件/配置资产为主 | `partial` | 需要影响下一次实际 workflow 执行 |
| 26 | MCP 工具治理 | `v0-1-0-release-readiness`、`ui-live-parity-hardening-v1` | routines/MCP registry 部分 UI/API | `/next/routines`、settings 部分展示 | 未进入统一 CLI/headless | routines E2E 只证明页面可达 | `partial` | 需要真实连接、列工具、权限、tool loop 调用和 transcript |
| 27 | Skills / prompt fragments | `conversation-parity-v1`、`v0-1-0-release-readiness` | routines/skills assets | `/next/routines` | 未统一 | 页面级证据 | `partial` | 需要被 command/workflow 引用并影响 prompt/context |
| 28 | Hooks 生命周期 | `v0-1-0-release-readiness` | routines hooks assets | `/next/routines` | 未统一 | 页面级证据 | `partial` | 需要 before/after turn/tool/candidate/chapter 生命周期执行 |
| 29 | 发布检查 | `platform-compliance-v1`、`novel-creation-workbench-complete-flow` | compliance routes、publish report | 工作台发布报告节点 | 未统一 | route/UI 证据 | `partial` | 可复用，但需进入 `/novel:publish-check` 和 headless |
| 30 | 去 AI 味 / 风格迁移 | `ai-taste-filter-v1`、`writing-modes-v1` | ai taste、style profile/tools | 工作台动作/工具资产 | 未统一 | core/API 证据为主 | `partial` | 需进入 `/novel:de-ai`、`/novel:style-transfer` 与候选稿边界 |
| 31 | 导出 Markdown/TXT | `novel-creation-workbench-complete-flow` | export route/UI | 工作台导出入口 | CLI 未统一 | route/UI/E2E 证据 | `current` | 当前可作为基础作品能力，但不是 Agent 对标核心 |
| 32 | Clean root / release smoke | `v0-1-0-release-readiness` | compiled smoke、Playwright clean root | compiled `/next` smoke | 无 | Task18 历史证据；Task21 暂停 | `partial` | 旧 release evidence 不再代表新 Agent 主线完成 |

---

## 5. 当前优先修复顺序

1. **Runtime 同源**：先把 Studio、CLI、headless 的 session、events、commands、tools、permissions、transcript 统一。
2. **设置/套路接实**：让设置和套路页真的影响 runtime，而不是只显示状态。
3. **`/novel:write-next` 闭环**：以叙述者为主入口，跑通 context → PGI → Guided Plan → approve → candidate → canvas。
4. **CLI/headless 同源验收**：同一写作任务必须能通过 `novelfork exec --output-format stream-json` 跑到同样边界。
5. **写作模式/工具回主流程**：选段写作和健康/叙事线工具回到当前画布与资源面。
6. **重定发布标准**：等端到端能力真实后，再决定恢复 v0.1.0 还是转向 v0.2.0。

---

## 6. 本矩阵的维护规则

- 每完成一个 `claude-codex-novel-agent-v1` 任务，必须更新相关行状态和证据。
- 能力从 `partial/not-wired/planned` 升为 `current` 前，必须补充 Studio 或 CLI/headless 端到端证据。
- 如果证据只有 API、组件、unit test，则最多只能标 `partial`。
- 如果能力只出现在 archive spec checkbox 中，当前路径不可用，则必须标 `not-wired`。
- 如果能力与 Claude/Codex 对标有关，必须说明真实源码/官方行为/可运行 CLI 证据来源。
