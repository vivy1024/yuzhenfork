# 当前执行主线

**版本**: v1.0.2
**创建日期**: 2026-04-28
**更新日期**: 2026-05-08
**状态**: ✅ 当前有效
**文档类型**: current

---

## 当前 spec 状态

| Spec | 角色 | 当前口径 |
|---|---|---|
| `.kiro/specs/novel-writing-features/` | 当前 active 小说写作功能主线 | 执行中（Phase 1-7 已完成，Phase 8 进行中）：新书引导向导、写作预设、AI 味检测、章节健康度、选段写作+变体、日更进度+节拍表、平台合规+导出、角色弧线+文风漂移+模板市场已全部接入新前端；Phase 8 浏览器端到端验证进行中。 |
| `.kiro/specs/claude-codex-novel-agent-v1/` | 当前 active 纠偏主线 | 执行中（14/48）：Phase 0、Phase 1 canonical runtime、session lifecycle、stream-json/NDJSON emitter、CLI/headless prompt path，以及 Phase 2 统一 command registry / runtime command executor / permission tool policy resolver / 确认门 checkpoint envelope / Codex sandbox-approval 状态模型已完成。本主线重新定义 NovelFork 为 ClaudeCodeCLI / CodexCLI 级 Agent Runtime + 小说创作端到端工具包。 |
| `.kiro/specs/backend-contract-v1/` | 历史资产：后端合同阶段 | 已完成（9/9）；已建立能力矩阵、typed contract client、session/resource/provider/writing action 领域 client、session WebSocket helper、资源树 adapter、写作动作 adapter、fetchJson 桥接与验证 guard。 |
| `.kiro/specs/frontend-refoundation-v1/` | 历史资产：前端重建阶段 | 已完成（12/12）；已正式计入 Backend Contract client、Agent Shell 路由壳、Conversation runtime、单栏 Conversation surface、模型/权限状态栏动作、Tool Result Renderer Registry、Workbench 资源树、canvas/viewer 与写作动作入口；`main.tsx` 与 `StudioNextApp.tsx` 主入口已切断旧三栏默认依赖，失败三栏实验已从 Studio typecheck 构建路径正式退役，并完成 route/retirement guard、app-next Vitest、TypeScript、docs verify 与浏览器冒烟。 |
| `.kiro/specs/frontend-live-wiring-v1/` | 历史资产：前端 live 接线阶段 | 已完成（10/10）；`/next/narrators/:sessionId` 已接 live conversation runtime、模型池/权限/推理配置、工具结果 renderer 与 session tool 确认刷新；`/next/books/:bookId` 已接 resource contract、保存、只读能力、透明 unsupported 状态、dirty canvasContext 与写作动作会话跳转；`/next/search`、`/next/routines`、`/next/settings` 已挂载真实页面或真实错误/透明 unsupported 状态，不再使用“稍后接线”占位。 |
| `.kiro/specs/legacy-source-retirement-v1/` | 历史资产：旧源码退役阶段 | 已完成（10/10）；旧三栏、旧 ChatWindow、未挂载 route 残留和 `/api/chat` 轻量 route 已删除，windowStore 会话事实源 consumer 已迁到 session runtime；exact `POST /api/agent` 已退役，`/api/pipeline`、`/api/monitor`、旧导出与旧 AI panel 继续按 process-memory/unsupported/deprecated 合同透明保留。 |
| `.kiro/specs/conversation-parity-v1/` | 历史资产：会话能力阶段 | ⚠️ 阶段性收口（13/13）；仅完成 NovelFork 自身会话能力 checklist 与 Claude Code CLI 2.1.69 参考基线，不代表完整对标；已完成会话 lifecycle service、ResumePicker/ForkDialog UI 接线、Slash Command Registry、`/compact` 产品化、Memory 写入边界、细粒度工具权限策略、Headless stream-json API 与 CLI 会话命令，支持 scoped continue latest、resume by id、归档 readonly/restore、fork session 标题/继承说明、Composer slash 建议、`/help`/`/status`/`/model`/`/permission`/`/fork`/`/resume`/`/compact` 结构化执行、compact summary/budget 展示、会话 memory 只读状态、偏好/项目事实/临时剧情草稿三分法审计、`sessionConfig.toolPolicy` allow/deny/ask 与 `policy-denied`/`permission-required`/`policy-disabled` 结构化结果，`POST /api/sessions/headless-chat` 的 text/stream-json input、NDJSON output、ephemeral/no-session-persistence、permission_request、max turns/budget stop result 与 duration/stop_reason/usage/cost unknown/permission_denials envelope，`novelfork chat` 的 text/json/stream-json/sessionId/pending confirmation 调用、正式章节/Truth/story/narrative apply 写入前 `.novelfork/checkpoints` 快照，以及按 checkpoint 预览 diff/hash/risk、确认门恢复、拒绝/冲突/资源移动失败与 rewind audit；最终文档当时已收口，但当前因用户反馈进入发布前重审；仍排除完整终端 TUI、tmux、Chrome bridge、remote-control 和插件市场，且不得再把这批 partial 能力写成 Claude/Codex 完整对标。 |
| `.kiro/specs/backend-core-refactor-v1/` | 后端核心整理阶段 | 已完成（10/10）；已建立 `contract-guard.md`，新增 `contract-regression.test.ts` 覆盖 books、sessions、providers、resources、writing actions 的成功、404/400、能力不可用/gate 与脱敏边界，并完成统一错误与状态 helper、storage.ts 只读/非破坏写入/destructive service 拆分、session runtime transport/recovery 内聚拆分、Provider/runtime store 边界收敛、legacy route 依赖调查/deprecated 标记，以及最终 API/存储/README/CHANGELOG 文档与验证收口。 |
| `.kiro/specs/ui-live-parity-hardening-v1/` | UI parity 硬化主线 | ⚠️ 阶段性收口（14/14）；已完成 Task 1 RED 回归基线、Task 2 资源详情 hydrate、Task 3 保存控制器/dirty guard、Task 4 浏览器资源编辑读回 E2E、Task 5 Shell session/recovery sync、Task 6 SettingsTruthModel/设置分组、Task 7 模型/Agent runtime 真实来源收敛、Task 8 provider callable 状态模型、Task 9 会话配置/对话 header facts、Task 10 工具/审批/运行控制透明化、Task 11 Claude Code parity baseline、Task 12 Codex CLI parity baseline 与 Task 13 设置页/会话浏览器 E2E。当前 `/next/books/:bookId` 打开章节/Story/Truth/草稿 preview 会先读取真实详情，保存后回读详情；`e2e/workbench-resource-edit.spec.ts` 已覆盖真实浏览器打开章节→修改→保存→刷新读回；工作台动作新建 session 会同步左侧叙述者列表，recovery notice 显示归一化状态和 cursor；模型与 Agent runtime 设置页现在按 SettingsFact 展示来源/状态/API，默认/摘要/Explore/Plan/General/Codex 推理强度均来自 user settings，WebFetch proxy 标明 `/api/proxy`，first-token timeout 因 schema 缺失标 planned，并在运行控制保存后回读 `/api/settings/user`；Provider 页区分 API key provider 可配置/已配置/已验证/可调用，平台无账号或 0 模型显示不可调用，运行总览拆分 total/available/callable 模型口径；对话 header 显示真实 binding、消息数、模型、权限、推理、context usage 与工作区/Git unavailable 原因，session config 更新后回读 chat state 并同步 ShellDataProvider；工具卡补复制/全屏/raw 脱敏，审批门展示目标/风险/来源/操作，运行控制只启用真实中断与已接 Compact，未接 Retry/Clear/Fork/Resume 显示 disabled reason；Claude/Codex parity baseline 仅作为参考基线和 UI claim guard，不能宣称完整对标；当前 NovelFork slash、permission、tool policy、headless 与 Codex sandbox/MCP/review/image 等能力仍需按源码/官方实现重新审计，发布前不得把 partial/planned 写成完成；`e2e/settings-session-conversation.spec.ts` 已覆盖 `/next/settings` 真实设置状态、工作台创建 session、Shell 同步、narrator route header/config 回读、工具卡 raw 脱敏，以及 idle/running 中断控制。2026-05-07 已追加 4567 NovelFork 与 7778 NarraFork 真实浏览器手工体验对比：确认 NovelFork 合同/状态透明已收敛，但 `/next/narrators/:sessionId` 视觉布局、composer、空态和侧栏会话管理仍未达 NarraFork 产品成熟度；这些发布阻塞项已上收进 `v0-1-0-release-readiness`，Task 14 已完成并将剩余发布门槛上收进 v0.1.0 release-readiness。 |
| `.kiro/specs/v0-1-0-release-readiness/` | 暂停：历史 release 准备主线 | ⏸️ 暂停（20/23）；Task21-23 不继续推进，等待 `claude-codex-novel-agent-v1` 重新定义 Agent 产品化完成标准；已完成 Task 1 发布准备基线与可追溯清单、Task 2 会话页成品化 RED 测试、Task 3 ConversationSurface 五区布局重排、Task 4 会话页浏览器 E2E 与检查点、Task 5 叙述者中心/Shell 左栏 RED 测试、Task 6 发布级叙述者中心与 Shell 左栏收敛、Task 7-8 产品壳首页 RED→GREEN、Task 9-10 作品工作台 RED→GREEN、Task 11 设置/Provider 可读性与 E2E 夹具污染治理、Task 12 Routines/工作流配置台验活、Task 13 clean/isolated root 主路径 E2E。会话页现已拆为 session header、runtime summary cards、recovery/confirmation lane、message stream、composer dock，新增作者向空态与 model-unavailable 恢复说明，并通过 Playwright 覆盖空会话、工具卡 raw/全屏、pending confirmation 拒绝回读、模型/权限/推理回读和 idle/running 中断控制；叙述者中心现在显示工作目录、创建/最后消息时间与排序控件，新建叙述者表单支持工作目录/绑定对象/模型/权限/计划模式，Shell 左栏只显示前 5 条并提供 `/next/sessions` 完整中心入口；`/next` 主区已从开发占位页升级为作者首页，展示最近作品、最近会话、模型健康、快速动作和空态；作品工作台现在显示作品标题/状态、资源 header 卡片、真实路径/读写能力/保存状态/只读原因和写作动作结果边界，并保留资源 hydrate、保存后回读、dirty guard 与写作动作 session 边界；设置页 Provider 列表支持搜索、异常过滤、隐藏测试夹具，并在开发 root 明确 E2E Provider 为测试夹具开发数据；`/next/routines` 已通过浏览器验活覆盖命令、工具、权限、技能、子代理、MCP、钩子分区；Playwright 现在通过 `NOVELFORK_RUNTIME_DIR` 隔离 provider/user runtime state，主路径 E2E 已覆盖 clean root 无 E2E/Planner 噪声、首页、作品、工作台、写作动作建 session、会话、设置、Provider、Routines 与关于。Task 14 clean root 手工软件验活、Task 15 旧 spec 收口、Task 16 全量自动化验证矩阵、Task 17 v0.1.0 版本资料/发布文档同步与 Task 18 编译产物 compiled smoke 已完成；Task19 源码对照重审已完成：已把 Claude slash/permission/session/headless 拆入 `CLAUDE_CODE_PARITY_MATRIX` 并降级为 partial/non-goal，新增 `reference-only` 状态并将 Codex exec JSONL event taxonomy 标为 reference-only；Task20 已完成反 mock/hardcoded/route literal 审计并将 backend-contract `/api/*` 路径集中到 `api-paths.ts`，新增守卫后 Studio Vitest 全量 213 files / 1274 tests、Studio typecheck、docs verify 与 diff check 通过；GitHub Release 未创建，`v0.1.0` tag 已撤回。Task21-23 不再作为当前下一步；后续如恢复 release，必须先由 `claude-codex-novel-agent-v1` 重新定义完成标准，并另开 release readiness 补充任务，不得直接恢复旧 Task21-23。 |
| `.kiro/specs/novelfork-ui-v1/` | 被取代的过渡参考 | 保留“sidebar + 全宽对话”的过渡方案参考，不再作为实现主线。 |
| `.kiro/specs/archive/agent-native-workspace-v1/` | 已完成历史主线 | 23/23 已完成；确立 session-first 工作台、session tools、工具结果 renderer、PGI / guided / candidate / Narrative Line 链路，并完成测试矩阵、non-goals 回归与最终收尾检查。 |
| `.kiro/specs/archive/novel-creation-workbench-complete-flow/` | 已完成历史主线 | Phase 0-7 已完成，覆盖小说创作工作台完整闭环、透明过渡收敛和最终维护验收。 |
| `.kiro/specs/archive/longform-cockpit-v1/` | 历史规划资产 | 长篇驾驶舱不再作为右侧主入口执行；可复用为 cockpit 工具结果卡片和中间画布组件。 |

其他 spec 已移入 `.kiro/specs/archive/`，只作为历史、参考或复用资产来源，不再作为当前任务入口。当前 active spec 索引以 `.kiro/specs/README.md` 为准。

## 已归档但仍需引用的事实来源

| 归档 spec | 当前用途 |
|---|---|
| `.kiro/specs/archive/docs-system-rearchitecture/` | 文档目录结构、迁移映射和 `docs:verify` 规则来源。 |
| `.kiro/specs/archive/project-wide-real-runtime-cleanup/` | 反 mock、transparent unsupported、mock debt ledger 与真实运行时边界来源。 |
| `.kiro/specs/archive/ui-quality-cleanup/` | app-next UI 清理历史和可复用 UI 约束来源。 |
| `.kiro/specs/archive/novel-bible-v1/`、`.kiro/specs/archive/onboarding-and-story-jingwei/` | 经纬、问卷、生成前追问、核心设定变更等底座参考。 |
| `.kiro/specs/archive/writing-modes-v1/`、`.kiro/specs/archive/writing-tools-v1/` | 写作模式和写作工具底座参考。 |

## 当前优先级

1. `novel-writing-features` 执行中（Phase 1-7 已完成，Phase 8 进行中）：小说写作功能全部接入新前端，包括新书引导向导、写作预设、AI 味检测、章节健康度、选段写作+变体、日更进度+节拍表、平台合规+导出、角色弧线+文风漂移+模板市场；下一步 Phase 8 浏览器端到端验证。
2. `claude-codex-novel-agent-v1` 执行中（14/48）：当前 active 纠偏主线。Phase 0、Phase 1 canonical runtime、session lifecycle、stream-json/NDJSON event emitter、CLI/headless prompt path，以及 Phase 2 统一 command registry / runtime command executor / permission tool policy resolver / 确认门 checkpoint envelope / Codex sandbox-approval 状态模型已完成；下一步按 tasks.md 推进设置/套路接实、`/novel:write-next` 端到端闭环与 CLI/headless 同源验收。
2. `ui-live-parity-hardening-v1` 阶段性收口（14/14）：资源详情 hydrate、保存防覆盖、浏览器保存刷新读回 E2E、Shell session/recovery sync、SettingsTruthModel、provider callable、会话透明化等 UI hardening 项已完成；但 Claude Code/Codex CLI 相关内容只能算参考基线和 UI claim guard，不是完整对标，已列为 v0.1.0 发布阻塞重审项。所有 UI current 声明必须绑定真实合同或浏览器证据，所有“体验达标”声明必须绑定真实浏览器截图/手工记录。
3. `conversation-parity-v1` 阶段性收口（13/13）：仅代表 NovelFork 自身会话能力 checklist 已完成，包括 continue/latest/resume、归档/恢复、fork service、Slash Command Registry、`/compact`、Memory 边界、toolPolicy、Headless stream-json、CLI 会话命令、checkpoint/rewind 与 usage envelope；不代表 Claude Code CLI / Codex CLI 完整对标，尤其 slash 动态注册、权限模式、MCP/插件/终端 TUI、Codex sandbox/review/image/MCP 等仍未按源码完整实现。
4. 维护 `backend-contract-v1` 合同边界：新增后端能力前先补矩阵、共享类型、contract client / adapter 与验证 guard，不在 app-next 组件内散写未登记 API。
5. 保持 `legacy-source-retirement-v1` 已完成口径：旧三栏、旧 ChatWindow、未挂载 route 残留、轻量 `/api/chat` 与 exact `POST /api/agent` 已退役；`/api/pipeline`、`/api/monitor`、旧导出入口、旧 AI 面板直连接口只允许标注 transparent/deprecated/unsupported 等非 current 状态，不得宣传为真实可用能力，恢复前必须重新创建 current contract、tests 与 consumer。
6. 保持 `agent-native-workspace-v1` 与 `web-agent-runtime-v1` 的历史资产口径：session tools、工具循环、确认门、Guided/PGI/candidate/Narrative Line 和 Web Agent Runtime 均作为后续重建资产复用；其 `current/partial/not-wired/planned/unsupported/non-goal` 产品重验状态以 [产品能力重新验收矩阵](./04-产品能力重新验收矩阵.md) 为准。
7. `backend-core-refactor-v1` 已完成 10/10：合同守护、核心回归测试、错误/状态 helper 收敛、storage 只读/非破坏写入/destructive service 拆分、session runtime transport/recovery 内聚拆分、Provider/runtime store 边界收敛、legacy route 依赖调查和最终文档/验收；后续新增后端能力仍必须先调查、再分阶段最小改动，不大规模破坏后端 route。
8. 保持 `novel-creation-workbench-complete-flow` Phase 0-7 的完成口径，不恢复 mock/fake/noop 假成功。
9. 历史长篇驾驶舱能力只作为 cockpit tools / 工具结果卡片 / 中间画布组件复用，不再恢复为右侧主入口。

## 执行约束

- 文档体系以 `docs/` 当前目录结构和 `.kiro/specs/README.md` 为准。
- 业务事实以当前主线 spec、Backend Contract、代码、测试和运行状态为准。
- current 文档只能描述当前真实状态；规划能力必须链接到对应主线任务。
- 未接入能力必须 transparent unsupported，不能假成功。

## 相关入口

- [文档体系规范](../00-文档治理/01-文档体系规范.md)
- [Studio能力矩阵](./02-Studio能力矩阵.md)
- [项目当前状态](./01-项目当前状态.md)
- [小说创作流程](../03-产品与流程/01-小说创作流程.md)
